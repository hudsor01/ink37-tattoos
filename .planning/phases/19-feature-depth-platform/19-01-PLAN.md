---
phase: 19-feature-depth-platform
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/data-table.tsx
  - src/components/dashboard/responsive-data-table.tsx
  - src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
  - src/app/(dashboard)/dashboard/gift-cards/gift-cards-client.tsx
  - src/app/(dashboard)/dashboard/contacts/contacts-client.tsx
  - src/app/(dashboard)/dashboard/sessions/session-list-client.tsx
  - src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
  - src/app/(dashboard)/dashboard/payments/page.tsx
  - src/app/(dashboard)/dashboard/products/page.tsx
  - src/app/(dashboard)/dashboard/orders/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/dashboard-client.tsx
  - src/lib/dal/analytics.ts
autonomous: true
requirements: [FEAT-13, FEAT-01]

must_haves:
  truths:
    - "Every list page in the dashboard has a CSV Export button in the toolbar"
    - "Every list page supports a Show All toggle alongside pagination"
    - "Every list page has a page-jump input to navigate to a specific page number"
    - "Dashboard shows today's appointments as clickable rows linking to appointment detail"
    - "Dashboard KPI cards show percentage change vs previous period with green/red arrows"
    - "Dashboard has 3-4 quick action buttons (New Appointment, New Customer, Upload Media, New Session)"
    - "Dashboard has a DateRangePicker wired to URL params controlling the KPI period"
  artifacts:
    - path: "src/components/dashboard/data-table.tsx"
      provides: "DataTable with CSV export, show-all, page-jump props"
      contains: "enableCsvExport"
    - path: "src/components/dashboard/responsive-data-table.tsx"
      provides: "ResponsiveDataTable passing through new DataTable props"
      contains: "enableCsvExport"
    - path: "src/app/(dashboard)/dashboard/dashboard-client.tsx"
      provides: "Dashboard client component with date range picker and KPIs"
      contains: "DateRangePicker"
    - path: "src/lib/dal/analytics.ts"
      provides: "getTodayAppointments and getDashboardStatsWithTrend DAL functions"
      exports: ["getTodayAppointments", "getDashboardStatsWithTrend"]
  key_links:
    - from: "src/components/dashboard/data-table.tsx"
      to: "src/lib/utils/csv-export.ts"
      via: "importToCsv for CSV export button"
      pattern: "exportToCsv"
    - from: "src/app/(dashboard)/dashboard/dashboard-client.tsx"
      to: "src/lib/dal/analytics.ts"
      via: "server component passes data to client"
      pattern: "getDashboardStatsWithTrend"
---

<objective>
Enhance DataTable with universal CSV export, show-all toggle, and page-jump controls (FEAT-13), then rebuild the dashboard overview with today's appointments, clickable KPI cards with period-over-period trends, quick actions, and date range filtering (FEAT-01).

Purpose: DataTable enhancements give every list page in the admin dashboard export and navigation controls. Dashboard overview gives the artist an at-a-glance daily view of their business with actionable widgets.
Output: Enhanced DataTable component wired to all 8 list pages, redesigned dashboard overview page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-feature-depth-platform/19-CONTEXT.md
@.planning/phases/19-feature-depth-platform/19-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/dashboard/data-table.tsx:
```typescript
interface DataTableProps<TData, TValue> {
  columns: ColumnDef<TData, TValue>[];
  data: TData[];
  searchKey?: string;
  searchPlaceholder?: string;
  pageSize?: number;
  enableRowSelection?: boolean;
  onRowSelectionChange?: (selectedRows: TData[]) => void;
  globalSearch?: boolean;
  facetFilters?: FacetFilter[];
}
```

From src/components/dashboard/responsive-data-table.tsx:
```typescript
interface ResponsiveDataTableProps<TData, TValue> {
  columns: ColumnDef<TData, TValue>[];
  data: TData[];
  searchKey?: string;
  searchPlaceholder?: string;
  pageSize?: number;
  enableRowSelection?: boolean;
  onRowSelectionChange?: (selectedRows: TData[]) => void;
  globalSearch?: boolean;
  facetFilters?: FacetFilter[];
  mobileFields: MobileField<TData>[];
  mobileTitle: (row: TData) => ReactNode;
  mobileActions?: (row: TData) => ReactNode;
}
```

From src/lib/utils/csv-export.ts:
```typescript
export function exportToCsv(filename: string, rows: Record<string, unknown>[]): void;
```

From src/components/dashboard/kpi-card.tsx:
```typescript
interface KPICardProps {
  title: string;
  value: string | number;
  description?: string;
  icon: LucideIcon;
  trend?: { value: number; label: string };
}
```

From src/components/dashboard/date-range-picker.tsx:
```typescript
// Reusable component with preset buttons (30d, 3m, 6m, 12m, YTD) and dual calendar
// Used in reports page: wired to URL search params
```

From src/lib/dal/analytics.ts:
```typescript
export const getDashboardStats = cache(async () => {...});
// Returns: { totalCustomers, totalAppointments, completedSessions, totalRevenue, recentAppointments }
export const getRevenueData = cache(async (months: number) => {...});
export const getPaymentMethodBreakdown = cache(async (startDate?, endDate?) => {...});
export const getRevenueByDateRange = cache(async (startDate, endDate) => {...});
```

All 8 list pages using DataTable/ResponsiveDataTable:
- customers/customer-list-client.tsx (DataTable)
- gift-cards/gift-cards-client.tsx (DataTable)
- contacts/contacts-client.tsx (DataTable)
- sessions/session-list-client.tsx (DataTable)
- appointments/appointment-list-client.tsx (ResponsiveDataTable)
- payments/page.tsx (ResponsiveDataTable)
- products/page.tsx (ResponsiveDataTable)
- orders/page.tsx (ResponsiveDataTable)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataTable CSV export, show-all toggle, page-jump controls + wire to all list pages</name>
  <files>
    src/components/dashboard/data-table.tsx,
    src/components/dashboard/responsive-data-table.tsx,
    src/app/(dashboard)/dashboard/customers/customer-list-client.tsx,
    src/app/(dashboard)/dashboard/gift-cards/gift-cards-client.tsx,
    src/app/(dashboard)/dashboard/contacts/contacts-client.tsx,
    src/app/(dashboard)/dashboard/sessions/session-list-client.tsx,
    src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx,
    src/app/(dashboard)/dashboard/payments/page.tsx,
    src/app/(dashboard)/dashboard/products/page.tsx,
    src/app/(dashboard)/dashboard/orders/page.tsx
  </files>
  <read_first>
    src/components/dashboard/data-table.tsx,
    src/components/dashboard/responsive-data-table.tsx,
    src/lib/utils/csv-export.ts,
    src/app/(dashboard)/dashboard/customers/customer-list-client.tsx,
    src/app/(dashboard)/dashboard/gift-cards/gift-cards-client.tsx,
    src/app/(dashboard)/dashboard/contacts/contacts-client.tsx,
    src/app/(dashboard)/dashboard/sessions/session-list-client.tsx,
    src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx,
    src/app/(dashboard)/dashboard/payments/page.tsx,
    src/app/(dashboard)/dashboard/products/page.tsx,
    src/app/(dashboard)/dashboard/orders/page.tsx
  </read_first>
  <action>
    Per D-11: Add three new optional props to DataTable interface in data-table.tsx:

    ```typescript
    enableCsvExport?: boolean;
    csvFilename?: string;
    csvTransform?: (data: TData[]) => Record<string, unknown>[];
    enableShowAll?: boolean;
    enablePageJump?: boolean;
    ```

    **CSV Export button (toolbar area):**
    - Import `exportToCsv` from `@/lib/utils/csv-export`, `Download` from lucide-react, and `toast` from sonner
    - Add a "Export CSV" button next to the existing column visibility dropdown
    - When clicked: if csvTransform provided, use it; otherwise build rows from visible columns (excluding 'select' and 'actions' columns), then call `exportToCsv(csvFilename ?? 'export.csv', rows)`
    - Export filtered rows (table.getFilteredRowModel()) not all rows
    - **Row limit (addresses review concern: CSV export memory):** Hard cap CSV export at 1,000 rows. Before exporting, check `filteredRows.length`. If > 1000, slice to first 1000 rows and show `toast.warning("Export limited to 1,000 rows. Apply filters to narrow your data.")`. When filtered rows > 1000, change button text to "Export CSV (1000 max)" so the user sees the limit before clicking.

    **Show All toggle (pagination footer):**
    - Add a ghost Button that toggles between "Show All" and "Paginate"
    - Store the original pageSize via useState initialized from the pageSize prop
    - **Hard cap at 500 rows (addresses review concern: show-all safety):** If `data.length <= 500`, "Show All" calls `table.setPageSize(data.length)`. If `data.length > 500`, cap at 500: `table.setPageSize(500)` and show `toast.warning("Showing first 500 of ${data.length} rows. Use filters or export for full dataset.")`. Button text shows "Show All (500 max)" when data.length > 500 so the user sees the cap before clicking. "Paginate" restores original pageSize.
    - Place between the row count text and the pagination controls

    **Page Jump input (pagination footer):**
    - Add an Input (type="number", min=1, max=pageCount) with "Go to:" label
    - Width w-16, height h-8, placed after the "Page X of Y" text
    - On Enter keypress: validate range 1..totalPages, call table.setPageIndex(page - 1)
    - Only show when pageCount > 1

    **ResponsiveDataTable passthrough:**
    - Add the 5 new props (enableCsvExport, csvFilename, csvTransform, enableShowAll, enablePageJump) to ResponsiveDataTableProps
    - Pass them through to the inner DataTable component

    **Wire to all 8 list pages:**
    For each page that uses DataTable or ResponsiveDataTable, add:
    - `enableCsvExport` with a descriptive `csvFilename` (e.g., "customers.csv", "appointments.csv")
    - `enableShowAll`
    - `enablePageJump`

    Files to add props to:
    1. customers/customer-list-client.tsx -- DataTable: csvFilename="customers.csv"
    2. gift-cards/gift-cards-client.tsx -- DataTable: csvFilename="gift-cards.csv"
    3. contacts/contacts-client.tsx -- DataTable: csvFilename="contacts.csv"
    4. sessions/session-list-client.tsx -- DataTable: csvFilename="sessions.csv"
    5. appointments/appointment-list-client.tsx -- ResponsiveDataTable: csvFilename="appointments.csv"
    6. payments/page.tsx -- ResponsiveDataTable: csvFilename="payments.csv"
    7. products/page.tsx -- ResponsiveDataTable: csvFilename="products.csv"
    8. orders/page.tsx -- ResponsiveDataTable: csvFilename="orders.csv"
  </action>
  <verify>
    <automated>
      cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | tail -5 && echo "---TSC DONE---" && grep -c "enableCsvExport" src/components/dashboard/data-table.tsx && grep -c "enableShowAll" src/components/dashboard/data-table.tsx && grep -c "enablePageJump" src/components/dashboard/data-table.tsx && grep -c "enableCsvExport" src/components/dashboard/responsive-data-table.tsx && echo "---PROPS IN COMPONENT---" && grep -l "enableCsvExport" src/app/\(dashboard\)/dashboard/customers/customer-list-client.tsx src/app/\(dashboard\)/dashboard/gift-cards/gift-cards-client.tsx src/app/\(dashboard\)/dashboard/contacts/contacts-client.tsx src/app/\(dashboard\)/dashboard/sessions/session-list-client.tsx src/app/\(dashboard\)/dashboard/appointments/appointment-list-client.tsx src/app/\(dashboard\)/dashboard/payments/page.tsx src/app/\(dashboard\)/dashboard/products/page.tsx src/app/\(dashboard\)/dashboard/orders/page.tsx 2>&1 && echo "---ALL 8 PAGES WIRED---" && grep -c "exportToCsv" src/components/dashboard/data-table.tsx && echo "---CSV IMPORT PRESENT---"
    </automated>
  </verify>
  <acceptance_criteria>
    - DataTable accepts enableCsvExport, csvFilename, csvTransform, enableShowAll, enablePageJump as optional props
    - CSV Export button renders in toolbar when enableCsvExport=true, calls exportToCsv with filtered rows
    - CSV export hard-capped at 1,000 rows with toast.warning when exceeded and button text shows "(1000 max)"
    - Show All hard-capped at 500 rows with toast.warning when data exceeds 500 and button text shows "(500 max)"
    - Page Jump input appears when pageCount > 1, navigates on Enter
    - ResponsiveDataTable passes all 5 new props through to inner DataTable
    - All 8 list pages have enableCsvExport, enableShowAll, enablePageJump set
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>All 8 dashboard list pages have CSV export (1000 row cap), show-all (500 row cap), and page-jump controls via DataTable prop enhancement</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard overview with today's appointments, trend KPIs, quick actions, and date range</name>
  <files>
    src/lib/dal/analytics.ts,
    src/app/(dashboard)/dashboard/page.tsx,
    src/app/(dashboard)/dashboard/dashboard-client.tsx
  </files>
  <read_first>
    src/lib/dal/analytics.ts,
    src/app/(dashboard)/dashboard/page.tsx,
    src/components/dashboard/kpi-card.tsx,
    src/components/dashboard/date-range-picker.tsx,
    src/components/dashboard/analytics-chart.tsx,
    src/app/(dashboard)/dashboard/reports/reports-client.tsx
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04: Rebuild the dashboard page with full overview features.

    **New DAL functions in src/lib/dal/analytics.ts:**

    1. `getTodayAppointments()` -- cached, requireStaffRole, queries appointments where scheduledDate is between start of today (midnight) and end of today (23:59:59). Use `startOfDay` and `endOfDay` from date-fns. Join with customer for firstName/lastName. Order by scheduledDate ascending. Return array of appointments with customer name.

    2. `getThisWeekAppointments()` -- cached, requireStaffRole, queries appointments for the current week (Monday to Sunday). Use `startOfWeek` and `endOfWeek` from date-fns with `weekStartsOn: 1`. Join with customer. Return array.

    3. `getDashboardStatsWithTrend(from: Date, to: Date)` -- cached, requireStaffRole. Computes stats for the given period AND the previous period of equal length. For each KPI (revenue, customers, appointments, completed sessions), return `{ value: number, trend: number }` where trend is the percentage change vs previous period. Use `calcTrendPercent(current, previous)`: if previous === 0, return current > 0 ? 100 : 0; otherwise Math.round(((current - previous) / previous) * 100). Run both period queries in parallel with Promise.all.

    **Dashboard page.tsx refactor:**
    - Convert to a thin server component that fetches initial data and renders DashboardClient
    - Parse searchParams for `from` and `to` date range (default: last 30 days)
    - Fetch: getTodayAppointments(), getThisWeekAppointments(), getDashboardStatsWithTrend(from, to), getRevenueByDateRange(from, to)
    - Pass all data to DashboardClient

    **New DashboardClient component (dashboard-client.tsx):**
    - 'use client' component receiving server data as props
    - Layout per D-01:
      - **Top section:** Today's Appointments card with a table showing time, customer name (clickable Link to /dashboard/appointments/{id}), type, status. If empty, show "No appointments today" message with a subtle CalendarX icon.
      - **This Week section (optional):** Show count badge "X appointments this week" if > 0
      - **KPI cards row (per D-02):** 4 cards in a 2x2 grid on mobile, 4-col on lg:
        - Total Revenue (DollarSign icon) -- clickable, links to /dashboard/reports
        - Total Customers (Users icon) -- clickable, links to /dashboard/customers
        - Appointments (Calendar icon) -- clickable, links to /dashboard/appointments
        - Sessions Completed (CheckCircle icon) -- clickable, links to /dashboard/sessions
        - Each card uses KPICard with trend prop: `{ value: trendPercent, label: 'vs prev period' }`
        - Wrap each KPICard in a Next.js Link component for clickability
      - **Quick Actions row (per D-03):** 3-4 Button components with icons:
        - "New Appointment" (CalendarPlus) -> /dashboard/appointments?action=new
        - "New Customer" (UserPlus) -> /dashboard/customers?action=new
        - "Upload Media" (Upload) -> /dashboard/media
        - "New Session" (Clock) -> /dashboard/sessions?action=new
        - Use outline variant buttons in a flex row, gap-3
      - **Bottom section:** Revenue trend chart (RevenueChart) with DateRangePicker per D-04
        - DateRangePicker wired to URL params using router.push with updated searchParams (same pattern as reports-client.tsx)
        - When date range changes, update URL params; server component re-fetches data

    **Responsive behavior (Claude's discretion):**
    - Mobile: stack all sections vertically, KPIs in 2-col grid, quick actions as 2x2 grid
    - Desktop: KPIs in 4-col row, quick actions in single row
  </action>
  <verify>
    <automated>
      cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | tail -5 && echo "---TSC DONE---" && grep -c "getTodayAppointments" src/lib/dal/analytics.ts && grep -c "getDashboardStatsWithTrend" src/lib/dal/analytics.ts && grep -c "getThisWeekAppointments" src/lib/dal/analytics.ts && echo "---DAL FUNCTIONS---" && grep -c "DateRangePicker" src/app/\(dashboard\)/dashboard/dashboard-client.tsx && grep -c "KPICard" src/app/\(dashboard\)/dashboard/dashboard-client.tsx && grep -c "trend" src/app/\(dashboard\)/dashboard/dashboard-client.tsx && echo "---DASHBOARD CLIENT---" && grep -c "DashboardClient" src/app/\(dashboard\)/dashboard/page.tsx && echo "---PAGE WIRED---"
    </automated>
  </verify>
  <acceptance_criteria>
    - getTodayAppointments returns appointments for today with customer names
    - getThisWeekAppointments returns appointments for the current Monday-Sunday week
    - getDashboardStatsWithTrend computes trend percentages for revenue, customers, appointments, sessions
    - Dashboard page passes date range from URL params to DAL functions
    - DashboardClient renders today's appointments as clickable rows
    - KPI cards show trend arrows (green up / red down) with percentage
    - KPI cards are wrapped in Links to their respective detail pages
    - Quick actions section has 3-4 buttons linking to create flows
    - DateRangePicker controls the KPI/chart period via URL params
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>Dashboard overview shows today's appointments, KPI cards with period-over-period trend percentages, quick action buttons, and date-range-filtered revenue chart</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` returns zero errors
2. All existing tests pass: `bunx vitest run --reporter=verbose`
3. DataTable has enableCsvExport, enableShowAll, enablePageJump props
4. All 8 list pages pass the 3 new DataTable props
5. CSV export capped at 1,000 rows with toast warning
6. Show All capped at 500 rows with toast warning
7. Dashboard page.tsx renders DashboardClient with date range from URL params
8. Analytics DAL has getTodayAppointments, getThisWeekAppointments, getDashboardStatsWithTrend
9. KPICard trend prop used on all 4 dashboard KPI cards
</verification>

<success_criteria>
- Every list page in the dashboard has a CSV Export button (1000 row limit), Show All toggle (500 row cap), and page-jump input (FEAT-13)
- Dashboard overview shows today's appointments, KPI cards with trend arrows, quick actions, and date range picker (FEAT-01)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-feature-depth-platform/19-01-SUMMARY.md`
</output>
