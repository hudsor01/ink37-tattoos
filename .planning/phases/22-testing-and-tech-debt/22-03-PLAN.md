---
phase: 22-testing-and-tech-debt
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/__tests__/api-cron.test.ts
  - src/__tests__/api-admin.test.ts
  - src/__tests__/api-portal.test.ts
  - src/__tests__/api-pdf-routes.test.ts
  - src/__tests__/api-notifications.test.ts
  - src/__tests__/api-calendar.test.ts
  - src/__tests__/api-store-download.test.ts
  - src/__tests__/webhook-malformed.test.ts
  - src/__tests__/rate-limiter-concurrent.test.ts
autonomous: true
requirements: [TEST-02, TEST-05, TEST-06]

must_haves:
  truths:
    - "Every API route has an integration test verifying auth enforcement"
    - "Every API route has an integration test verifying input validation"
    - "Every API route has an integration test verifying correct error status codes"
    - "Webhook handlers tested with malformed payloads and missing required fields"
    - "Rate limiter correctly limits under concurrent Promise.all load"
  artifacts:
    - path: "src/__tests__/api-cron.test.ts"
      provides: "Cron route tests (balance-due, no-show-followup) with Bearer token auth"
    - path: "src/__tests__/api-admin.test.ts"
      provides: "Admin API route tests (customers, media, appointments, sessions)"
    - path: "src/__tests__/api-portal.test.ts"
      provides: "Portal billing route tests with rate limiting"
    - path: "src/__tests__/api-pdf-routes.test.ts"
      provides: "PDF generation route tests (invoices, receipts, analytics export)"
    - path: "src/__tests__/api-notifications.test.ts"
      provides: "Notifications API route tests"
    - path: "src/__tests__/api-calendar.test.ts"
      provides: "Calendar API route tests"
    - path: "src/__tests__/api-store-download.test.ts"
      provides: "Store download route tests with rate limiting"
    - path: "src/__tests__/webhook-malformed.test.ts"
      provides: "Webhook malformed payload tests for Stripe, Cal.com, Resend"
    - path: "src/__tests__/rate-limiter-concurrent.test.ts"
      provides: "Rate limiter concurrent load tests"
  key_links:
    - from: "src/__tests__/api-cron.test.ts"
      to: "src/app/api/cron/*/route.ts"
      via: "dynamic import + process.env.CRON_SECRET"
      pattern: "await import.*api/cron"
    - from: "src/__tests__/api-admin.test.ts"
      to: "src/app/api/admin/*/route.ts"
      via: "dynamic import + getCurrentSession mock"
      pattern: "await import.*api/admin"
    - from: "src/__tests__/webhook-malformed.test.ts"
      to: "src/app/api/webhooks/*/route.ts"
      via: "dynamic import + malformed Request objects"
      pattern: "await import.*webhooks"
    - from: "src/__tests__/rate-limiter-concurrent.test.ts"
      to: "src/lib/security/rate-limiter.ts"
      via: "direct import + Promise.all"
      pattern: "Promise\\.all.*rateLimiters"
---

<objective>
Add integration tests for all untested API routes, webhook malformed payload tests, and rate limiter concurrent load tests.

Purpose: TEST-02 requires integration tests for every API route. TEST-05 requires webhook tests with malformed payloads and concurrent duplicates. TEST-06 requires rate limiter concurrent load tests. The existing tests cover upload and webhook routes but not cron, admin, portal, PDF, notifications, calendar, or store download routes.
Output: 9 new test files covering all API routes, webhook edge cases, and concurrent rate limiting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-testing-and-tech-debt/22-RESEARCH.md
@.planning/phases/22-testing-and-tech-debt/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API route integration tests (7 files)</name>
  <files>
    src/__tests__/api-cron.test.ts,
    src/__tests__/api-admin.test.ts,
    src/__tests__/api-portal.test.ts,
    src/__tests__/api-pdf-routes.test.ts,
    src/__tests__/api-notifications.test.ts,
    src/__tests__/api-calendar.test.ts,
    src/__tests__/api-store-download.test.ts
  </files>
  <read_first>
    src/app/api/cron/balance-due/route.ts,
    src/app/api/cron/no-show-followup/route.ts,
    src/app/api/admin/customers/route.ts,
    src/app/api/admin/media/route.ts,
    src/app/api/admin/appointments/route.ts,
    src/app/api/admin/sessions/route.ts,
    src/app/api/admin/calendar/route.ts,
    src/app/api/portal/billing/route.ts,
    src/app/api/invoices/[paymentId]/route.ts,
    src/app/api/receipts/[paymentId]/route.ts,
    src/app/api/analytics/export/pdf/route.ts,
    src/app/api/notifications/route.ts,
    src/app/api/store/download/route.ts,
    src/__tests__/upload-endpoint.test.ts
  </read_first>
  <action>
    Create 7 new API route test files. For each route, test at minimum:
    1. **Auth enforcement:** Unauthenticated request returns 401
    2. **Role enforcement (if applicable):** Insufficient role returns 403
    3. **Input validation:** Invalid/missing params returns 400
    4. **Success path:** Valid request returns correct status (200/201) and response shape

    Use the established API route test pattern from upload-endpoint.test.ts:
    - Create helper `makeRequest(url, opts)` function
    - Mock `getCurrentSession` from `@/lib/auth`
    - Mock NextResponse.json for response construction
    - Dynamic import of route inside each it() block
    - vi.clearAllMocks in beforeEach

    **Per-file specifics:**

    **api-cron.test.ts:** Cron routes use Bearer token auth via process.env.CRON_SECRET (NOT getCurrentSession). Set process.env.CRON_SECRET in beforeEach, delete in afterEach. Test:
    - Missing Authorization header -> 401
    - Wrong Bearer token -> 401
    - Valid token + success -> 200
    - Mock DAL functions for balance-due and no-show-followup logic

    **api-admin.test.ts:** Admin routes (/api/admin/customers, /api/admin/media, /api/admin/appointments, /api/admin/sessions) require admin role check. Test:
    - No session -> 401
    - Non-admin role -> 403
    - Valid admin + query params -> 200 with paginated results
    - Mock the DAL list/search functions these routes call

    **api-portal.test.ts:** Portal billing route has rate limiting. Test:
    - No session -> 401
    - Rate limit exceeded -> 429
    - Valid request -> correct billing response
    - Mock rateLimiters.portalBilling

    **api-pdf-routes.test.ts:** Invoice, receipt, and analytics export routes call Stirling PDF service. Test:
    - No session -> 401
    - Invalid paymentId -> 400 or 404
    - Mock global fetch for Stirling PDF HEAD health check and POST conversion
    - Use `vi.stubGlobal('fetch', mockFetch)` for PDF service mocking
    - Success -> returns PDF blob or appropriate response

    **api-notifications.test.ts:** Notifications route. Test:
    - No session -> 401
    - Valid session -> 200 with notifications array
    - Mock DAL notification list function

    **api-calendar.test.ts:** Calendar route uses DAL requireStaffRole. Test:
    - No session -> 401
    - Non-staff -> 403
    - Valid staff + date range params -> 200 with appointment data
    - Mock DAL calendar/appointment functions

    **api-store-download.test.ts:** Store download route has rate limiting. Test:
    - Rate limit exceeded -> 429
    - Invalid/missing token -> 400/404
    - Valid download token -> file/redirect response
    - Mock rateLimiters.storeDownload and download token validation

    **CRITICAL patterns (from RESEARCH.md):**
    - Cron routes read process.env.CRON_SECRET directly, NOT via env() helper
    - PDF routes use global fetch() -- use vi.stubGlobal to mock
    - Admin routes may check ADMIN_ROLES directly or use DAL auth
    - Always mock 'server-only'
    - Always mock next/server NextResponse
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx vitest run src/__tests__/api-cron.test.ts src/__tests__/api-admin.test.ts src/__tests__/api-portal.test.ts src/__tests__/api-pdf-routes.test.ts src/__tests__/api-notifications.test.ts src/__tests__/api-calendar.test.ts src/__tests__/api-store-download.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 7 new API route test files pass with 0 failures
    - Every API route has tests for auth enforcement, input validation, and correct error status codes
    - Cron routes tested with Bearer token pattern (not session-based auth)
    - PDF routes tested with mocked Stirling PDF fetch
    - Rate-limited routes tested for 429 responses
    - Full test suite (`npx vitest run`) still passes with 0 failures
  </acceptance_criteria>
  <done>All API routes in src/app/api/ have integration test coverage. TEST-02 complete.</done>
</task>

<task type="auto">
  <name>Task 2: Webhook malformed payload tests + rate limiter concurrent load tests</name>
  <files>
    src/__tests__/webhook-malformed.test.ts,
    src/__tests__/rate-limiter-concurrent.test.ts
  </files>
  <read_first>
    src/app/api/webhooks/stripe/route.ts,
    src/app/api/webhooks/cal/route.ts,
    src/app/api/webhooks/resend/route.ts,
    src/lib/security/rate-limiter.ts,
    src/__tests__/webhook-stripe.test.ts,
    src/__tests__/rate-limiter.test.ts
  </read_first>
  <action>
    **webhook-malformed.test.ts (TEST-05):**

    Create a dedicated test file for malformed webhook payload testing. For each webhook handler (Stripe, Cal.com, Resend), test:

    1. **Empty body:** Send empty string/null body -> handler should return error response (not crash)
    2. **Missing required fields:** Send valid JSON structure but with null/undefined required fields (e.g., Stripe event with data.object = null, Cal.com payload with missing triggerEvent)
    3. **Wrong types:** Send fields with wrong types (string where number expected, etc.)
    4. **Invalid JSON:** Send malformed JSON body -> handler should return 400
    5. **Concurrent duplicate events (Stripe):** Send the same event ID twice with Promise.all -> only one should process, second should be idempotent

    For Stripe: Mock stripe.webhooks.constructEvent to return events with malformed data. Test that the route handles gracefully (returns 200 or 400, never 500 crash).

    For Cal.com: Mock the CalWebhookPayloadSchema validation. Send payloads missing triggerEvent, attendees, booking data. Test Zod validation rejection.

    For Resend: Send webhook with missing or invalid event type. Test graceful handling.

    **rate-limiter-concurrent.test.ts (TEST-06):**

    Create a dedicated concurrent load test for the rate limiter. Import rateLimiters from @/lib/security/rate-limiter.

    1. **Concurrent burst test:** Fire N requests simultaneously with Promise.all where N exceeds the rate limit. Verify exactly the limit number succeed and the rest fail.
    2. **Different IPs concurrent:** Fire requests from multiple IPs concurrently. Verify each IP has independent limits.
    3. **Window reset:** Fire up to the limit, wait for window to pass (or mock time), verify limit resets.

    Test each rate limiter variant (contact, portalBilling, storeDownload) with their configured limits.

    Use unique IP addresses per test (e.g., `test-${Date.now()}-${Math.random()}`) to prevent cross-test contamination.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx vitest run src/__tests__/webhook-malformed.test.ts src/__tests__/rate-limiter-concurrent.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - webhook-malformed.test.ts passes with tests for all 3 webhook handlers (Stripe, Cal.com, Resend) with malformed payloads
    - Concurrent duplicate Stripe event test verifies idempotency
    - rate-limiter-concurrent.test.ts passes with Promise.all concurrent load tests
    - Rate limiter correctly reports successes/failures matching configured limits
    - Full test suite (`npx vitest run`) still passes with 0 failures
  </acceptance_criteria>
  <done>TEST-05 (webhook malformed/concurrent tests) and TEST-06 (rate limiter concurrent load) complete. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/api-*.test.ts --reporter=verbose` -- all API route tests pass
- `npx vitest run src/__tests__/webhook-malformed.test.ts --reporter=verbose` -- malformed webhook tests pass
- `npx vitest run src/__tests__/rate-limiter-concurrent.test.ts --reporter=verbose` -- concurrent load tests pass
- `npx vitest run --reporter=verbose` -- full suite green, 0 failures
- `ls src/__tests__/api-*.test.ts | wc -l` returns 7+ (all API route domains covered)
</verification>

<success_criteria>
Every API route has integration tests for auth, validation, and error responses (TEST-02). Webhook handlers tested with malformed payloads, missing fields, and concurrent duplicates (TEST-05). Rate limiter verified under concurrent load (TEST-06). All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/22-testing-and-tech-debt/22-03-SUMMARY.md`
</output>
