---
phase: 22-testing-and-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/server-actions.test.ts
  - src/__tests__/cal-webhook.test.ts
  - src/__tests__/contact-form.test.ts
  - src/__tests__/webhook-stripe.test.ts
  - src/components/dashboard/session-form.tsx
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04]

must_haves:
  truths:
    - "All 399 existing tests pass (0 failures)"
    - "DEBT-01 confirmed resolved (0 asChild occurrences)"
    - "DEBT-02 confirmed resolved (contacts page exists)"
    - "DEBT-03 confirmed resolved (audit log uses Shadcn Select)"
    - "DEBT-04 resolved -- session-form.tsx uses Shadcn FormField wrapper pattern"
  artifacts:
    - path: "src/__tests__/server-actions.test.ts"
      provides: "Fixed portal action mocks matching current code"
    - path: "src/__tests__/cal-webhook.test.ts"
      provides: "Fixed Cal.com webhook mocks with Zod validation + calEvent + notifications"
    - path: "src/__tests__/contact-form.test.ts"
      provides: "Fixed contact form action mocks matching current code"
    - path: "src/__tests__/webhook-stripe.test.ts"
      provides: "Fixed Stripe webhook mock covering payment processing chain"
    - path: "src/components/dashboard/session-form.tsx"
      provides: "Session form using Shadcn Form wrapper (FormField/FormItem/FormControl)"
  key_links:
    - from: "src/__tests__/server-actions.test.ts"
      to: "src/lib/actions/portal-actions.ts"
      via: "dynamic import + vi.mock"
      pattern: "await import.*portal-actions"
    - from: "src/__tests__/cal-webhook.test.ts"
      to: "src/app/api/webhooks/cal/route.ts"
      via: "dynamic import + vi.mock"
      pattern: "await import.*webhooks/cal"
    - from: "src/components/dashboard/session-form.tsx"
      to: "src/components/ui/form.tsx"
      via: "Shadcn Form import"
      pattern: "import.*Form.*from.*ui/form"
---

<objective>
Fix all 21 currently failing tests and close remaining tech debt items to establish a green baseline for new test coverage.

Purpose: No new test coverage can be reliably written on top of a broken test suite. Fixing the 21 failures from stale mocks (caused by Phases 13-21 code changes) ensures a clean baseline. DEBT-04 (session form conversion) is the only debt item requiring actual code changes.
Output: Green test suite (399/399 passing), all 4 DEBT items verified/resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-testing-and-tech-debt/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 21 failing tests across 4 test files</name>
  <files>
    src/__tests__/server-actions.test.ts,
    src/__tests__/cal-webhook.test.ts,
    src/__tests__/contact-form.test.ts,
    src/__tests__/webhook-stripe.test.ts
  </files>
  <read_first>
    src/lib/actions/portal-actions.ts,
    src/app/api/webhooks/cal/route.ts,
    src/lib/actions/contact-actions.ts,
    src/lib/actions/contact-status-action.ts,
    src/app/api/webhooks/stripe/route.ts,
    src/__tests__/server-actions.test.ts,
    src/__tests__/cal-webhook.test.ts,
    src/__tests__/contact-form.test.ts,
    src/__tests__/webhook-stripe.test.ts
  </read_first>
  <action>
    Fix all 21 test failures by updating stale mocks to match current production code.

    **server-actions.test.ts (6 failures):**
    - Read portal-actions.ts to understand current auth pattern (requireRole vs getCurrentSession vs local requireRole)
    - Read the current signConsentAction and updateProfileAction signatures and imports
    - Update mocks to match the exact import pattern and function signatures in portal-actions.ts
    - Ensure the mock for auth matches how portal-actions.ts actually calls it (e.g., if it uses getCurrentSession instead of requireRole, mock that)
    - Check if actions now use safeAction wrapper and update expected return shapes accordingly

    **cal-webhook.test.ts (6 failures):**
    - Read the current Cal.com webhook route to identify all imports and dependencies
    - Add mocks for CalWebhookPayloadSchema (from @/lib/security/validation)
    - Add mocks for calEvent table insert chain (the route now inserts into calEvent table)
    - Add mocks for createNotificationForAdmins (from @/lib/dal/notifications)
    - Update the mock Drizzle chain to include any new .onConflictDoNothing() or .returning() calls

    **contact-form.test.ts (7 failures):**
    - Read contact-actions.ts and contact-status-action.ts to understand current implementation
    - Update mocks to match current DAL function names, validation schemas, and return shapes
    - Check if rate limiting was added to the action and mock accordingly
    - Check if audit logging was added and mock next/server after()

    **webhook-stripe.test.ts (1 failure):**
    - Read the current Stripe webhook route handler for payment_intent.succeeded
    - The "processes new events" test returns 500 instead of 200 -- the handler throws because the mock does not cover the full processing chain
    - Add mocks for payment lookup, update, and any notification/email sending the handler now performs
    - Ensure the mock covers the complete Drizzle query chain the handler uses

    **CRITICAL anti-patterns (from RESEARCH.md):**
    - Do NOT use vi.hoisted() -- use module-scope const mocks
    - Do NOT forget to mock 'server-only'
    - Do NOT forget to mock next/server after() as vi.fn((fn) => fn())
    - Use dynamic import inside it() blocks when mocks vary per test
    - Always call vi.clearAllMocks() in beforeEach
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx vitest run src/__tests__/server-actions.test.ts src/__tests__/cal-webhook.test.ts src/__tests__/contact-form.test.ts src/__tests__/webhook-stripe.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 21 previously failing tests now pass
    - No new test failures introduced
    - Full suite: `npx vitest run` shows 399/399 passing (or more if tests were added), 0 failures
  </acceptance_criteria>
  <done>All 4 test files pass. Full test suite is green (0 failures).</done>
</task>

<task type="auto">
  <name>Task 2: Close tech debt -- DEBT-04 session form + verify DEBT-01/02/03</name>
  <files>
    src/components/dashboard/session-form.tsx
  </files>
  <read_first>
    src/components/dashboard/session-form.tsx,
    src/components/ui/form.tsx,
    src/components/dashboard/customer-form.tsx,
    src/components/dashboard/appointment-form.tsx
  </read_first>
  <action>
    **DEBT-04: Convert session-form.tsx from raw register() to Shadcn Form wrapper pattern.**

    Read customer-form.tsx and appointment-form.tsx to see the established Shadcn Form wrapper pattern used throughout the app. These forms use:
    - `<Form {...form}>` wrapper around the entire form
    - `<FormField control={form.control} name="fieldName" render={({field}) => ...}` for each field
    - `<FormItem>`, `<FormLabel>`, `<FormControl>`, `<FormMessage>` hierarchy
    - zodResolver for validation

    Convert session-form.tsx from its current raw `register()` pattern to match this established pattern:
    1. Replace `const { register, handleSubmit, ... } = useForm()` with `const form = useForm()` pattern
    2. Wrap form JSX in `<Form {...form}>`
    3. Replace each raw `<Input {...register('fieldName')} />` with the FormField/FormItem/FormControl pattern
    4. Replace each raw `<Select {...register('fieldName')}>` with FormField-wrapped Select
    5. Replace each raw `<Textarea {...register('fieldName')} />` with FormField-wrapped Textarea
    6. Keep the existing zodResolver, onSubmit handler, default values, and all field names exactly as-is
    7. Keep the existing conditional rendering logic (edit mode, loading states, etc.)
    8. Keep the form functional -- same fields, same validation, same submission behavior

    **DEBT-01 verification:** Run `grep -r "asChild" src/` -- expect 0 results. Mark complete.
    **DEBT-02 verification:** Confirm src/app/(dashboard)/dashboard/contacts/page.tsx exists. Mark complete.
    **DEBT-03 verification:** Run `grep -rn "Select" src/app/(dashboard)/dashboard/audit/` -- expect Shadcn Select imports. Mark complete.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && grep -c "FormField" src/components/dashboard/session-form.tsx && grep -c "register(" src/components/dashboard/session-form.tsx && grep -rc "asChild" src/ | grep -v ":0$" | head -5; echo "---"; npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - session-form.tsx uses FormField pattern (grep "FormField" returns 10+ matches)
    - session-form.tsx has 0 raw register() calls
    - asChild grep returns 0 matches across entire src/
    - Contacts page exists at src/app/(dashboard)/dashboard/contacts/page.tsx
    - Audit log page imports from @/components/ui/select (Shadcn Select)
    - Full test suite still passes
  </acceptance_criteria>
  <done>All 4 DEBT items verified/resolved. Session form uses Shadcn Form wrapper. Test suite remains green.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass, 0 failures
- `grep -r "asChild" src/` returns nothing (DEBT-01)
- `ls src/app/(dashboard)/dashboard/contacts/page.tsx` exists (DEBT-02)
- `grep "Select" src/app/(dashboard)/dashboard/audit/` shows Shadcn Select (DEBT-03)
- `grep "FormField" src/components/dashboard/session-form.tsx` returns matches (DEBT-04)
- `grep -c "register(" src/components/dashboard/session-form.tsx` returns 0 (DEBT-04)
</verification>

<success_criteria>
Full test suite green (399+ tests, 0 failures). All 4 DEBT items verified complete. Clean baseline for Wave 2 test expansion.
</success_criteria>

<output>
After completion, create `.planning/phases/22-testing-and-tech-debt/22-01-SUMMARY.md`
</output>
