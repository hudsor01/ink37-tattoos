---
phase: 15-ui-foundations
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-11
  - UI-13
gap_closure: true
must_haves:
  truths:
    - "REQUIREMENTS.md checkboxes for UI-06, UI-07, UI-08, UI-09, UI-11, UI-13 show [x] (Complete)"
    - "REQUIREMENTS.md traceability table entries for the same requirements show Complete status"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement status tracking"
      contains: "[x] **UI-06**"
  key_links: []
---

<objective>
Update REQUIREMENTS.md to mark completed UI requirements as done. Six requirements (UI-06, UI-07, UI-08, UI-09, UI-11, UI-13) are implemented in the codebase but still show as Pending in the tracking document.

Purpose: Accurate requirement tracking prevents confusion for future phases and agents. The verification report confirmed all six are implemented.

Output: REQUIREMENTS.md with correct checkbox status for all Phase 15 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/15-ui-foundations/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md checkboxes and traceability table for completed UI requirements</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/15-ui-foundations/15-VERIFICATION.md
  </read_first>
  <action>
Update two sections of `.planning/REQUIREMENTS.md`:

**Section 1: UI Quality checklist (lines 40-53)**

Change the following lines from `- [ ]` to `- [x]`:

- Line 45: `- [ ] **UI-04**` -- Leave as `[ ]` (NOT complete -- this is what Plan 06 fixes, and won't be complete until after execution)
- Line 46: `- [ ] **UI-06**` -> `- [x] **UI-06**`: All forms show field-level validation errors below inputs
- Line 47: `- [ ] **UI-07**` -> `- [x] **UI-07**`: All destructive actions use AlertDialog confirmation
- Line 48: `- [ ] **UI-08**` -> `- [x] **UI-08**`: All date inputs use date picker components
- Line 49: `- [ ] **UI-09**` -> `- [x] **UI-09**`: Consistent toast patterns across all mutations
- Line 51: `- [ ] **UI-11**` -> `- [x] **UI-11**`: Unsaved changes warning on all forms
- Line 53: `- [ ] **UI-13**` -> `- [x] **UI-13**`: StatusBadge uses theme-aware color tokens

**Section 2: Traceability table (lines 170-182)**

Update the Status column for these same requirements:

- UI-04: Leave as `Pending` (gap closure in progress)
- UI-06: Change `Pending` to `Complete`
- UI-07: Change `Pending` to `Complete`
- UI-08: Change `Pending` to `Complete`
- UI-09: Change `Pending` to `Complete`
- UI-11: Change `Pending` to `Complete`
- UI-13: Change `Pending` to `Complete`

**Do NOT change any other lines.** Leave UI-04 as Pending/unchecked -- it depends on Plan 06 completing the ResponsiveDataTable wiring.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*UI-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\[x\] \*\*UI-06\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "\[x\] \*\*UI-07\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "\[x\] \*\*UI-08\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "\[x\] \*\*UI-09\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "\[x\] \*\*UI-11\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "\[x\] \*\*UI-13\*\*" .planning/REQUIREMENTS.md` returns a match
    - `grep "\[ \] \*\*UI-04\*\*" .planning/REQUIREMENTS.md` returns a match (still pending)
    - `grep "UI-06.*Complete" .planning/REQUIREMENTS.md` returns a match in traceability table
    - `grep "UI-04.*Pending" .planning/REQUIREMENTS.md` returns a match in traceability table
    - Total `[x]` UI requirements count is 11 (UI-01, UI-02, UI-03, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10, UI-11, UI-12, UI-13 = 12 minus UI-04 = 11... wait: UI-01 [x], UI-02 [x], UI-03 [x], UI-04 [ ], UI-05 [x], UI-06 [x], UI-07 [x], UI-08 [x], UI-09 [x], UI-10 [x], UI-11 [x], UI-12 [x], UI-13 [x] = 12 checked)
    - `grep -c "\[x\] \*\*UI-" .planning/REQUIREMENTS.md` returns 12
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately reflects implementation status: 12 of 13 UI requirements marked complete, UI-04 remains pending until ResponsiveDataTable gap closure is executed.</done>
</task>

</tasks>

<verification>
- `grep -c "\[x\] \*\*UI-" .planning/REQUIREMENTS.md` returns 12
- `grep -c "\[ \] \*\*UI-" .planning/REQUIREMENTS.md` returns 1 (UI-04 only)
- UI-06, UI-07, UI-08, UI-09, UI-11, UI-13 all show Complete in traceability table
</verification>

<success_criteria>
REQUIREMENTS.md checkboxes and traceability table accurately reflect the current implementation status of all Phase 15 requirements. No false positives (UI-04 stays pending). No false negatives (6 previously-pending requirements now correctly marked complete).
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-foundations/15-07-SUMMARY.md`
</output>
