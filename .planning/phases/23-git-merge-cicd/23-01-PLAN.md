---
phase: 23-git-merge-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [GIT-01, GIT-02, GIT-03]

must_haves:
  truths:
    - "All 226 v2.0 commits from phase branches are on main"
    - "No worktree-agent-* branches exist locally or remotely"
    - "No gsd/phase-* branches exist locally or remotely"
    - "No stale miscellaneous branches remain"
    - "GitHub PR documents the v2.0 merge for project history"
  artifacts:
    - path: "main branch"
      provides: "All v2.0 work merged"
      contains: "226 commits from gsd/phase-22"
  key_links:
    - from: "gsd/phase-22-testing-and-tech-debt"
      to: "main"
      via: "GitHub PR merge"
      pattern: "git log main --oneline"
---

<objective>
Merge all v2.0 work to main via a clean GitHub PR and delete every stale branch.

Purpose: The entire v2.0 codebase (226 commits across 10 phase branches) lives only on feature branches. Nothing can deploy until main has this work. Branch cleanup removes 49 worktree-agent branches, 11 gsd/phase branches, and 4 miscellaneous stale branches.

Output: main branch with all v2.0 work, zero stale branches, clean GitHub PR for the record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push phase-22 branch and create v2.0 merge PR</name>
  <read_first>
    - Run `git log --oneline main..gsd/phase-22-testing-and-tech-debt | head -5` to confirm branch state
    - Run `git branch -a | grep -c worktree-agent` to count branches to clean
  </read_first>
  <files></files>
  <action>
1. Push the gsd/phase-22-testing-and-tech-debt branch to origin:
   `git push origin gsd/phase-22-testing-and-tech-debt`

2. Create a GitHub PR from gsd/phase-22-testing-and-tech-debt to main using gh CLI:
   - Title: "Merge v2.0 Admin Panel to main"
   - Body should summarize: 226 commits, 10 phases (13-22), key features added (security hardening, data layer, UI foundations, missing pages, feature depth, business workflows, analytics, testing)
   - Use `gh pr create --base main --head gsd/phase-22-testing-and-tech-debt`

3. Merge the PR immediately (no review required -- solo developer, all work is verified):
   `gh pr merge --merge --delete-branch`
   Use --merge (not --squash) to preserve the full commit history for traceability.

4. After merge, switch to main and pull:
   `git checkout main && git pull origin main`

5. Verify merge: `git log --oneline -5` should show the merge commit and recent v2.0 commits.
  </action>
  <verify>
    <automated>git log --oneline main | head -5 | grep -q "Merge" && echo "PASS: merge commit found on main" || echo "FAIL: no merge commit"</automated>
  </verify>
  <acceptance_criteria>
    - PR exists on GitHub documenting the v2.0 merge
    - main branch contains all 226 v2.0 commits
    - Currently checked out on main branch
  </acceptance_criteria>
  <done>main branch has all v2.0 work via a merged GitHub PR</done>
</task>

<task type="auto">
  <name>Task 2: Delete all stale branches (worktree-agent, gsd/phase, misc)</name>
  <read_first>
    - Run `git worktree list` to see active worktrees that need removal first
    - Run `git branch | grep worktree-agent` to list local worktree branches
    - Run `git branch | grep gsd/phase` to list local phase branches
  </read_first>
  <files></files>
  <action>
Must be on main branch before starting (verified in Task 1).

1. Remove all worktrees first (must happen before branch deletion):
   ```
   git worktree list | grep -v "^/Users/richard/Developer/ink37-tattoos " | awk '{print $1}' | xargs -I{} git worktree remove --force {}
   ```
   Then prune any stale worktree references: `git worktree prune`

2. Delete all local worktree-agent-* branches:
   ```
   git branch | grep "worktree-agent" | xargs git branch -D
   ```

3. Delete all local gsd/phase-* branches:
   ```
   git branch | grep "gsd/phase-" | xargs git branch -D
   ```

4. Delete miscellaneous stale local branches:
   ```
   git branch -D phase-12-schemas-msw phase-12-testing-foundation port-inner-pages
   ```

5. Delete remote stale branches:
   ```
   git push origin --delete gsd/phase-08-drizzle-migration frontend-consolidation phase-12-schemas-msw phase-12-testing-foundation port-inner-pages
   ```
   Note: gsd/phase-22 remote branch should already be deleted by the PR merge --delete-branch flag. If any remote deletion fails (branch already gone), that is fine -- continue.

6. Prune remote tracking references: `git remote prune origin`

7. Verify clean state: `git branch -a` should show only `main` and `remotes/origin/main` (plus remotes/origin/HEAD).
  </action>
  <verify>
    <automated>LOCAL=$(git branch | grep -v "main" | grep -v "^\*" | wc -l | tr -d ' '); REMOTE=$(git branch -r | grep -v "origin/main" | grep -v "origin/HEAD" | wc -l | tr -d ' '); echo "Stale local: $LOCAL, Stale remote: $REMOTE"; [ "$LOCAL" -eq 0 ] && [ "$REMOTE" -eq 0 ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Zero worktree-agent-* branches remain (local or remote)
    - Zero gsd/phase-* branches remain (local or remote)
    - Zero miscellaneous stale branches remain
    - Only main branch exists locally
    - Only origin/main exists remotely
  </acceptance_criteria>
  <done>All stale branches deleted from local and remote, only main remains</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify merge and branch cleanup on GitHub</name>
  <files></files>
  <action>
Human verification checkpoint. Claude automated the merge and branch cleanup in Tasks 1-2. User verifies the result on GitHub.

1. Visit the GitHub repository and confirm the merged PR is visible
2. Check the repository's branch list -- should show only main
3. Verify main branch shows recent v2.0 commits in the commit history
  </action>
  <verify>
    <automated>git branch -a | grep -v "main" | grep -v "HEAD" | grep -v "^\*" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - GitHub PR for v2.0 merge is visible in the repo
    - GitHub branch list shows only main
    - main branch commit history includes v2.0 work
  </acceptance_criteria>
  <done>User confirmed merge and branch cleanup look correct on GitHub</done>
</task>

</tasks>

<verification>
- `git branch -a` shows only main and remotes/origin/main (+ HEAD)
- `git log --oneline main | head -10` shows v2.0 commits
- `git worktree list` shows only the main worktree
- GitHub PR exists documenting the merge
</verification>

<success_criteria>
All 226 v2.0 commits are on main. Zero stale branches exist anywhere. GitHub PR documents the merge for project history.
</success_criteria>

<output>
After completion, create `.planning/phases/23-git-merge-cicd/23-01-SUMMARY.md`
</output>
