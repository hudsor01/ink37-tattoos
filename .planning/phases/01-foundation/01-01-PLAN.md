---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - prisma.config.ts
  - prisma/schema.prisma
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/env.ts
  - src/lib/db.ts
  - src/lib/utils.ts
  - .env.example
  - .gitignore
  - vitest.config.ts
  - src/__tests__/env.test.ts
  - src/__tests__/schema.test.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "Prisma 7 schema validates and generates a typed client with all unified models"
    - "All packages are at target versions (Next.js 16, React 19.2, Prisma 7, Zod 4, Tailwind 4)"
    - "Environment validation catches missing required vars and passes with valid ones"
    - "next build completes with zero errors"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Unified schema with all Phase 1 models"
      contains: "model Customer"
    - path: "src/lib/db.ts"
      provides: "Prisma 7 client singleton with driver adapter"
      contains: "PrismaPg"
    - path: "src/lib/env.ts"
      provides: "Zod 4 environment validation"
      contains: "z.object"
    - path: "package.json"
      provides: "All dependencies at target versions"
      contains: "next"
  key_links:
    - from: "src/lib/db.ts"
      to: "prisma/schema.prisma"
      via: "Prisma generated client import"
      pattern: "from.*generated/prisma"
    - from: "src/lib/env.ts"
      to: ".env.example"
      via: "Same variable names validated"
      pattern: "DATABASE_URL"
---

<objective>
Create the Next.js 16 project scaffold with all dependencies at target versions, the unified Prisma 7 schema (superset merge of both source repos), Prisma client singleton, Zod 4 environment validation, and Tailwind CSS 4 setup. This is the foundation every other plan builds on.

Purpose: Establish the base project that compiles and has the complete data model.
Output: A buildable Next.js 16 project with typed Prisma client and validated env config.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project with all dependencies and config</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, prisma.config.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/lib/utils.ts, .env.example, .gitignore, vitest.config.ts</files>
  <read_first>
    ../tattoo-website/package.json
    ../admin-tattoo-website/package.json
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create the project from scratch (no `create-next-app` -- manual setup for full control).

1. **package.json** -- Create with `"type": "module"` (required for Prisma 7 ESM). Install exact versions:
   - Core: `next@16.2.0`, `react@19.2.3`, `react-dom@19.2.3`
   - Database: `@prisma/client@7.5.0`, `@prisma/adapter-pg@7.5.0`, `pg`
   - Auth: `better-auth@1.5.5`
   - Validation: `zod@4.3.6`
   - State: `@tanstack/react-query@5.91.3`, `zustand@5.0.12`
   - Server boundary: `server-only`
   - UI utils: `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `next-themes`, `sonner`, `date-fns`
   - Dev: `prisma@7.5.0`, `tailwindcss@4.2.2`, `@tailwindcss/postcss@4.2.2`, `postcss`, `typescript@5.9.3`, `@types/node`, `@types/react`, `@types/react-dom`, `@types/pg`, `eslint`, `eslint-config-next@16.2.0`
   - Test: `vitest`, `@vitejs/plugin-react`
   - Scripts: `"dev": "next dev --turbopack"`, `"build": "next build"`, `"start": "next start"`, `"lint": "next lint"`, `"test": "vitest run --reporter=verbose"`, `"db:generate": "prisma generate"`, `"db:push": "prisma db push"`, `"db:migrate": "prisma migrate dev"`

2. **tsconfig.json** -- TypeScript config with:
   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "lib": ["dom", "dom.iterable", "esnext"],
       "allowJs": true,
       "skipLibCheck": true,
       "strict": true,
       "noEmit": true,
       "esModuleInterop": true,
       "module": "ESNext",
       "moduleResolution": "bundler",
       "resolveJsonModule": true,
       "isolatedModules": true,
       "jsx": "preserve",
       "incremental": true,
       "plugins": [{ "name": "next" }],
       "paths": { "@/*": ["./src/*"] }
     },
     "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
     "exclude": ["node_modules"]
   }
   ```

3. **next.config.ts** -- Minimal config:
   ```typescript
   import type { NextConfig } from 'next';
   const nextConfig: NextConfig = {};
   export default nextConfig;
   ```

4. **postcss.config.mjs** -- Tailwind v4 PostCSS:
   ```javascript
   const config = { plugins: { "@tailwindcss/postcss": {} } };
   export default config;
   ```

5. **prisma.config.ts** -- Prisma 7 runtime config:
   ```typescript
   import path from 'node:path';
   import { defineConfig } from 'prisma/config';
   export default defineConfig({
     earlyAccess: true,
     schema: path.join(import.meta.dirname, 'prisma', 'schema.prisma'),
     migrate: { async resolveUrl() { return process.env.DATABASE_URL!; } },
   });
   ```

6. **src/app/globals.css** -- Tailwind v4 CSS-first config:
   ```css
   @import "tailwindcss";
   @theme {
     --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
   }
   ```

7. **src/app/layout.tsx** -- Root layout with html/body, imports globals.css, sets metadata `title: "Ink37 Tattoos"`, `description: "Professional tattoo studio"`.

8. **src/app/page.tsx** -- Minimal placeholder: `<h1>Ink37 Tattoos</h1><p>Foundation phase</p>`.

9. **src/lib/utils.ts** -- Standard cn utility:
   ```typescript
   import { type ClassValue, clsx } from "clsx";
   import { twMerge } from "tailwind-merge";
   export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
   ```

10. **.env.example** -- Document all required env vars:
    ```
    DATABASE_URL=postgresql://user:pass@host:5432/ink37
    BETTER_AUTH_SECRET=your-secret-min-32-chars
    BETTER_AUTH_URL=http://localhost:3000
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    ```

11. **.gitignore** -- Standard Next.js gitignore plus `.env`, `.env.local`, `node_modules/`, `.next/`, `src/generated/`.

12. **vitest.config.ts** -- Test config:
    ```typescript
    import { defineConfig } from 'vitest/config';
    import react from '@vitejs/plugin-react';
    import path from 'node:path';
    export default defineConfig({
      plugins: [react()],
      test: { environment: 'node', globals: true },
      resolve: { alias: { '@': path.resolve(__dirname, './src') } },
    });
    ```

Run `npm install` after creating package.json.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npm install && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"type": "module"`
    - package.json contains `"next": "16.2.0"`
    - package.json contains `"react": "19.2.3"`
    - package.json contains `"zod": "4.3.6"`
    - package.json contains `"prisma": "7.5.0"` in devDependencies
    - package.json contains `"better-auth": "1.5.5"`
    - tsconfig.json contains `"module": "ESNext"`
    - tsconfig.json contains `"moduleResolution": "bundler"`
    - prisma.config.ts contains `defineConfig`
    - postcss.config.mjs contains `@tailwindcss/postcss`
    - src/app/globals.css contains `@import "tailwindcss"`
    - .env.example contains `DATABASE_URL=`
    - .env.example contains `BETTER_AUTH_SECRET=`
    - vitest.config.ts exists
    - node_modules/ directory exists after npm install
  </acceptance_criteria>
  <done>All packages installed at target versions, TypeScript compiles, project scaffold complete with Tailwind v4 CSS config, Prisma 7 runtime config, and test framework ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create unified Prisma 7 schema, DB client, and env validation</name>
  <files>prisma/schema.prisma, src/lib/db.ts, src/lib/env.ts, src/__tests__/env.test.ts, src/__tests__/schema.test.ts</files>
  <read_first>
    ../tattoo-website/prisma/schema.prisma
    ../admin-tattoo-website/prisma/schema.prisma
    .planning/phases/01-foundation/01-RESEARCH.md
    prisma.config.ts
  </read_first>
  <action>
1. **prisma/schema.prisma** -- Create the unified schema with Prisma 7 syntax:

   Generator and datasource:
   ```prisma
   datasource db {
     provider = "postgresql"
   }
   generator client {
     provider = "prisma-client"
     output   = "../src/generated/prisma"
   }
   ```

   **Better Auth models** (UUID IDs, use Better Auth CLI-expected table names -- run `npx @better-auth/cli generate` first to confirm, but based on research these are the expected names):

   - **User**: `id String @id @default(uuid())`, `name String`, `email String @unique`, `emailVerified Boolean @default(false)`, `image String?`, `role String @default("user")`, `banned Boolean @default(false)`, `banReason String?`, `banExpires DateTime?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, relations to Account[], Session[], AuditLog[]. `@@map("user")`

   - **Session**: `id String @id @default(uuid())`, `userId String`, `token String @unique`, `expiresAt DateTime`, `ipAddress String?`, `userAgent String?`, `impersonatedBy String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, relation to User. `@@map("session")`

   - **Account**: `id String @id @default(uuid())`, `userId String`, `accountId String`, `providerId String`, `type String @default("credential")`, `accessToken String?`, `refreshToken String?`, `accessTokenExpiresAt DateTime?`, `refreshTokenExpiresAt DateTime?`, `scope String?`, `idToken String?`, `password String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`, relation to User. `@@unique([providerId, accountId])` `@@map("account")`

   - **Verification**: `id String @id @default(uuid())`, `identifier String`, `value String`, `expiresAt DateTime`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. `@@unique([identifier, value])` `@@map("verification")`

   **Domain models** (all UUID IDs):

   - **Customer** (superset merge): `id String @id @default(uuid())`, `firstName String`, `lastName String`, `email String? @unique`, `phone String?`, `userId String? @unique` (FK to User for portal linking Phase 4), `dateOfBirth DateTime?`, `address String?`, `city String?`, `state String?`, `postalCode String?`, `country String?`, `emergencyName String?`, `emergencyPhone String?`, `emergencyRel String?`, `allergies String[]`, `medicalConditions String[]`, `preferredArtist String?`, `notes String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Relations: `user User?`, `appointments Appointment[]`, `tattooSessions TattooSession[]`, `designs TattooDesign[]`. `@@map("customer")`

   - **Appointment** (merge of admin appointments + public Booking): `id String @id @default(uuid())`, `customerId String`, `artistId String?`, `scheduledDate DateTime`, `duration Int?`, `status AppointmentStatus @default(PENDING)`, `type AppointmentType @default(CONSULTATION)`, `calBookingUid String? @unique`, `calEventTypeId Int?`, `calStatus String?`, `calMeetingUrl String?`, `firstName String`, `lastName String`, `email String`, `phone String?`, `tattooType String?`, `size String?`, `placement String?`, `description String?`, `source String @default("website")`, `notes String?`, `reminderSent Boolean @default(false)`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Relations: `customer Customer`, `artist TattooArtist?`, `session TattooSession?`. `@@map("appointment")`

   - **TattooSession** (from admin tattoo_sessions): `id String @id @default(uuid())`, `customerId String`, `artistId String`, `appointmentId String? @unique`, `appointmentDate DateTime`, `duration Int`, `status SessionStatus @default(SCHEDULED)`, `designDescription String`, `placement String`, `size String`, `style String`, `referenceImages String[]`, `hourlyRate Decimal`, `estimatedHours Decimal`, `depositAmount Decimal @default(0)`, `totalCost Decimal`, `paidAmount Decimal @default(0)`, `notes String?`, `aftercareProvided Boolean @default(false)`, `consentSigned Boolean @default(false)`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Relations: `customer Customer`, `artist TattooArtist`, `appointment Appointment?`. `@@map("tattoo_session")`

   - **TattooDesign** (unified): `id String @id @default(uuid())`, `name String`, `description String?`, `fileUrl String`, `thumbnailUrl String?`, `designType String?`, `size String?`, `style String?`, `tags String[]`, `isApproved Boolean @default(false)`, `isPublic Boolean @default(true)`, `estimatedHours Decimal?`, `popularity Int @default(0)`, `artistId String`, `customerId String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Relations: `artist TattooArtist`, `customer Customer?`. `@@map("tattoo_design")`

   - **TattooArtist**: `id String @id @default(uuid())`, `name String`, `email String @unique`, `phone String?`, `specialties String[]`, `hourlyRate Decimal`, `isActive Boolean @default(true)`, `portfolio String[]`, `bio String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. Relations: `appointments Appointment[]`, `sessions TattooSession[]`, `designs TattooDesign[]`. `@@map("tattoo_artist")`

   - **Contact**: `id String @id @default(uuid())`, `name String`, `email String`, `phone String?`, `message String`, `status ContactStatus @default(NEW)`, `adminNotes String?`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. `@@map("contact")`

   - **AuditLog**: `id String @id @default(uuid())`, `userId String?`, `action String`, `resource String`, `resourceId String?`, `ip String`, `userAgent String`, `timestamp DateTime @default(now())`, `metadata Json?`. Relation: `user User?`. `@@map("audit_log")`

   - **Settings**: `id String @id @default(uuid())`, `key String @unique`, `value Json`, `category String`, `description String?`, `isEnvironment Boolean @default(false)`, `createdAt DateTime @default(now())`, `updatedAt DateTime @updatedAt`. `@@map("settings")`

   **Enums:**
   - `AppointmentStatus`: PENDING, CONFIRMED, SCHEDULED, IN_PROGRESS, COMPLETED, CANCELLED, NO_SHOW
   - `AppointmentType`: CONSULTATION, DESIGN_REVIEW, TATTOO_SESSION, TOUCH_UP, REMOVAL
   - `SessionStatus`: SCHEDULED, IN_PROGRESS, COMPLETED, CANCELLED, NO_SHOW
   - `ContactStatus`: NEW, READ, REPLIED, RESOLVED

   Add appropriate `@@index` directives on frequently queried fields (scheduledDate, status, email, createdAt).

   **OMIT:** Payment model (Phase 3), RateLimit model (platform-level), all Cal analytics models (CalAnalyticsEvent, CalBookingFunnel, CalServiceAnalytics, CalRealtimeMetrics).

2. **src/lib/db.ts** -- Prisma 7 client singleton with driver adapter:
   ```typescript
   import 'server-only';
   import { PrismaClient } from '@/generated/prisma';
   import { PrismaPg } from '@prisma/adapter-pg';

   const globalForPrisma = globalThis as unknown as { prisma: PrismaClient | undefined };

   function createPrismaClient() {
     const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL! });
     return new PrismaClient({ adapter });
   }

   export const db = globalForPrisma.prisma ?? createPrismaClient();

   if (process.env.NODE_ENV !== 'production') {
     globalForPrisma.prisma = db;
   }
   ```

3. **src/lib/env.ts** -- Zod 4 environment validation:
   ```typescript
   import 'server-only';
   import { z } from 'zod';

   const envSchema = z.object({
     DATABASE_URL: z.url(),
     BETTER_AUTH_SECRET: z.string().min(32),
     BETTER_AUTH_URL: z.url(),
     NEXT_PUBLIC_APP_URL: z.url(),
     CAL_API_KEY: z.string().optional(),
     NEXT_PUBLIC_CAL_USERNAME: z.string().optional(),
     RESEND_API_KEY: z.string().optional(),
     VERCEL_BLOB_READ_WRITE_TOKEN: z.string().optional(),
   });

   export type Env = z.infer<typeof envSchema>;
   export const env = envSchema.parse(process.env);
   ```

4. **src/__tests__/env.test.ts** -- Test env validation:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { z } from 'zod';

   // Replicate the schema here to test without server-only import
   const envSchema = z.object({
     DATABASE_URL: z.url(),
     BETTER_AUTH_SECRET: z.string().min(32),
     BETTER_AUTH_URL: z.url(),
     NEXT_PUBLIC_APP_URL: z.url(),
     CAL_API_KEY: z.string().optional(),
     RESEND_API_KEY: z.string().optional(),
   });

   describe('Environment validation', () => {
     it('passes with valid env vars', () => {
       const result = envSchema.safeParse({
         DATABASE_URL: 'https://db.neon.tech/ink37',
         BETTER_AUTH_SECRET: 'a'.repeat(32),
         BETTER_AUTH_URL: 'http://localhost:3000',
         NEXT_PUBLIC_APP_URL: 'http://localhost:3000',
       });
       expect(result.success).toBe(true);
     });

     it('fails when DATABASE_URL is missing', () => {
       const result = envSchema.safeParse({
         BETTER_AUTH_SECRET: 'a'.repeat(32),
         BETTER_AUTH_URL: 'http://localhost:3000',
         NEXT_PUBLIC_APP_URL: 'http://localhost:3000',
       });
       expect(result.success).toBe(false);
     });

     it('fails when BETTER_AUTH_SECRET is too short', () => {
       const result = envSchema.safeParse({
         DATABASE_URL: 'https://db.neon.tech/ink37',
         BETTER_AUTH_SECRET: 'short',
         BETTER_AUTH_URL: 'http://localhost:3000',
         NEXT_PUBLIC_APP_URL: 'http://localhost:3000',
       });
       expect(result.success).toBe(false);
     });
   });
   ```

5. **src/__tests__/schema.test.ts** -- Validate schema generates:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { execSync } from 'node:child_process';

   describe('Prisma schema', () => {
     it('validates without errors', () => {
       const result = execSync('npx prisma validate', { encoding: 'utf-8' });
       expect(result).toContain('valid');
     });

     it('generates client without errors', () => {
       expect(() => {
         execSync('npx prisma generate', { encoding: 'utf-8', stdio: 'pipe' });
       }).not.toThrow();
     });
   });
   ```

Run `npx prisma generate` after creating the schema to produce the typed client.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx prisma validate && npx prisma generate && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `provider = "prisma-client"`
    - prisma/schema.prisma contains `output = "../src/generated/prisma"`
    - prisma/schema.prisma contains `model Customer`
    - prisma/schema.prisma contains `model Appointment`
    - prisma/schema.prisma contains `model TattooSession`
    - prisma/schema.prisma contains `model TattooDesign`
    - prisma/schema.prisma contains `model TattooArtist`
    - prisma/schema.prisma contains `model Contact`
    - prisma/schema.prisma contains `model AuditLog`
    - prisma/schema.prisma contains `model Settings`
    - prisma/schema.prisma contains `@default(uuid())`
    - prisma/schema.prisma does NOT contain `model Payment`
    - prisma/schema.prisma does NOT contain `model CalAnalyticsEvent`
    - prisma/schema.prisma does NOT contain `model RateLimit`
    - src/lib/db.ts contains `import 'server-only'`
    - src/lib/db.ts contains `PrismaPg`
    - src/lib/db.ts contains `from '@/generated/prisma'`
    - src/lib/env.ts contains `import 'server-only'`
    - src/lib/env.ts contains `DATABASE_URL`
    - src/lib/env.ts contains `BETTER_AUTH_SECRET`
    - src/generated/prisma directory exists after prisma generate
    - `npx prisma validate` exits with code 0
    - `npx vitest run` shows env and schema tests passing
  </acceptance_criteria>
  <done>Unified Prisma 7 schema validates and generates typed client. All models from both source repos merged (Customer superset, Appointment/Session split, Auth tables unified with UUID). Env validation tested. No Payment, RateLimit, or Cal analytics models included.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- `npx prisma generate` exits 0
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx tsc --noEmit` -- zero TypeScript errors
- package.json has all target versions
- Schema contains all 11 models (User, Session, Account, Verification, Customer, Appointment, TattooSession, TattooDesign, TattooArtist, Contact, AuditLog, Settings)
- Schema omits Payment, RateLimit, Cal analytics models
</verification>

<success_criteria>
- Next.js 16 project with all dependencies at exact target versions installs and compiles
- Prisma 7 unified schema covers all domain models with UUID IDs
- Generated Prisma client is type-safe and importable from @/generated/prisma
- Zod 4 env validation catches missing/invalid vars (tested)
- Tailwind CSS 4 is configured with CSS-first @theme approach
- Vitest test framework is set up and passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
