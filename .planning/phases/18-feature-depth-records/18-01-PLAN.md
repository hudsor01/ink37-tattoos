---
phase: 18-feature-depth-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/inline-edit.tsx
  - src/components/dashboard/bulk-action-toolbar.tsx
  - src/components/dashboard/csv-import-dialog.tsx
  - src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
  - src/app/(dashboard)/dashboard/customers/[id]/page.tsx
  - src/app/(dashboard)/dashboard/customers/[id]/customer-detail-client.tsx
  - src/lib/actions/customer-actions.ts
  - src/lib/dal/customers.ts
autonomous: true
requirements: [FEAT-02, FEAT-03]

must_haves:
  truths:
    - "Artist can select multiple customers via checkboxes and bulk delete them with confirmation"
    - "Artist can export selected (or all) customers to CSV"
    - "Artist can import customers from a CSV file with preview and email-based duplicate detection"
    - "Artist can click any text field on the customer detail page to edit it inline"
    - "Artist can create an appointment or session from the customer detail page"
    - "Customer detail page shows communication timeline (appointments + contacts) and portal account indicator"
  artifacts:
    - path: "src/components/dashboard/inline-edit.tsx"
      provides: "Reusable click-to-edit component with save/cancel"
      exports: ["InlineEdit"]
    - path: "src/components/dashboard/bulk-action-toolbar.tsx"
      provides: "Floating toolbar for selected row actions"
      exports: ["BulkActionToolbar"]
    - path: "src/components/dashboard/csv-import-dialog.tsx"
      provides: "CSV upload dialog with preview and duplicate detection"
      exports: ["CsvImportDialog"]
    - path: "src/lib/actions/customer-actions.ts"
      provides: "Bulk delete, CSV import server actions"
      exports: ["bulkDeleteCustomersAction", "importCustomersAction"]
    - path: "src/app/(dashboard)/dashboard/customers/[id]/customer-detail-client.tsx"
      provides: "Client component wrapping customer detail with inline editing"
      exports: ["CustomerDetailClient"]
  key_links:
    - from: "src/components/dashboard/bulk-action-toolbar.tsx"
      to: "customer-list-client.tsx"
      via: "Rendered when selectedRows.length > 0"
      pattern: "selectedRows\\.length"
    - from: "src/components/dashboard/csv-import-dialog.tsx"
      to: "src/lib/actions/customer-actions.ts"
      via: "importCustomersAction server action"
      pattern: "importCustomersAction"
    - from: "src/app/(dashboard)/dashboard/customers/[id]/customer-detail-client.tsx"
      to: "src/lib/actions/customer-actions.ts"
      via: "updateCustomerAction for inline edits"
      pattern: "updateCustomerAction"
---

<objective>
Add full operational depth to customer list and customer detail pages -- bulk actions (select, delete, export CSV), CSV import with duplicate detection, and inline editing with communication timeline on the detail page.

Purpose: FEAT-02 and FEAT-03 give the artist efficient customer management workflows instead of one-at-a-time operations. Also creates shared InlineEdit and BulkActionToolbar components reused by session detail page in Plan 02.
Output: Enhanced customer list with bulk ops, enhanced customer detail with inline editing, 3 reusable components.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-feature-depth-records/18-RESEARCH.md

@src/lib/actions/customer-actions.ts
@src/lib/dal/customers.ts
@src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
@src/app/(dashboard)/dashboard/customers/[id]/page.tsx
@src/lib/actions/safe-action.ts
@src/lib/actions/types.ts
@src/lib/utils/csv-export.ts
@src/lib/db/schema.ts
@src/components/dashboard/data-table.tsx

<interfaces>
From src/lib/actions/types.ts:
```typescript
export type ActionResult<T = void> =
  | { success: true; data: T }
  | { success: false; error: string; fieldErrors?: Record<string, string[]> };
```

From src/lib/dal/customers.ts:
```typescript
export const getCustomers = cache(async (opts) => ...);
export const getCustomerById = cache(async (id: string) => ...);
export const getCustomerWithDetails = cache(async (id: string) => ...);
export async function createCustomer(data) { ... }
export async function updateCustomer(id: string, data) { ... }
export async function deleteCustomer(id: string) { ... }
```

From src/lib/actions/customer-actions.ts:
```typescript
export async function createCustomerAction(formData: FormData): Promise<ActionResult<{ id: string }>>;
export async function updateCustomerAction(id: string, formData: FormData): Promise<ActionResult<{ id: string }>>;
export async function deleteCustomerAction(id: string): Promise<ActionResult<void>>;
```

From src/lib/utils/csv-export.ts:
```typescript
export function exportToCsv(filename: string, rows: Record<string, unknown>[]): void;
```

Customer schema fields: id, firstName, lastName, email, phone, userId (portal link), dateOfBirth, address, city, state, postalCode, country, emergencyName, emergencyPhone, emergencyRel, allergies, medicalConditions, preferredArtist, notes, stripeCustomerId, createdAt, updatedAt.

Customer relations: user (portal account), appointments, tattooSessions, designs, payments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared components + bulk action server actions</name>
  <files>
    src/components/dashboard/inline-edit.tsx,
    src/components/dashboard/bulk-action-toolbar.tsx,
    src/components/dashboard/csv-import-dialog.tsx,
    src/lib/actions/customer-actions.ts,
    src/lib/dal/customers.ts
  </files>
  <read_first>
    src/lib/actions/customer-actions.ts,
    src/lib/dal/customers.ts,
    src/lib/actions/safe-action.ts,
    src/lib/actions/types.ts,
    src/lib/db/schema.ts (lines 115-147 for customer table),
    src/lib/utils/csv-export.ts,
    src/components/dashboard/data-table.tsx (first 60 lines for selection pattern)
  </read_first>
  <action>
    **1. Create InlineEdit component** at `src/components/dashboard/inline-edit.tsx` (per D-06):
    - 'use client' component implementing click-to-edit with save/cancel pattern
    - Props: `value: string`, `onSave: (newValue: string) => Promise<void>`, `label: string`, `type?: 'text' | 'textarea' | 'select' | 'date'`, `options?: { value: string; label: string }[]` (for select type)
    - Display mode: renders value as a button with hover pencil icon (Pencil from lucide-react, opacity-0 group-hover:opacity-100)
    - Edit mode: shows Input (or Textarea/Select based on type) with Check (save) and X (cancel) icon buttons
    - Saving state: disable save button, show spinner
    - If `editValue === value`, just close without calling onSave
    - Use Shadcn Input, Textarea, Select components

    **2. Create BulkActionToolbar component** at `src/components/dashboard/bulk-action-toolbar.tsx` (per D-01):
    - 'use client' component that appears as a fixed bottom toolbar when rows are selected
    - Props: `selectedCount: number`, `onDelete: () => void`, `onExport: () => void`, `isDeleting?: boolean`
    - Show: "{N} selected" label, "Export CSV" button, "Delete" destructive button
    - Delete triggers an AlertDialog confirmation before calling onDelete
    - Animate in/out with CSS transition (translate-y)
    - "Deselect all" button that calls a provided `onClearSelection` callback

    **3. Create CsvImportDialog component** at `src/components/dashboard/csv-import-dialog.tsx` (per D-02):
    - 'use client' component using Dialog
    - Install papaparse: `npm install papaparse && npm install -D @types/papaparse`
    - File upload input accepting .csv files
    - On file select, parse with `Papa.parse(file, { header: true, skipEmptyLines: true })`
    - Show preview table of first 10 rows with columns: firstName, lastName, email, phone
    - Flexible column mapping -- if CSV headers differ, show a mapping dropdown for each required field (firstName, lastName) and optional fields (email, phone, address, etc.)
    - "Check Duplicates" button that sends email list to server to detect existing customers
    - Show duplicate rows highlighted in amber with "Duplicate" badge
    - "Import" button calls `importCustomersAction` with the mapped/validated rows (excluding duplicates unless user opts in)
    - Loading state during import with progress count

    **4. Add bulk server actions** to `src/lib/actions/customer-actions.ts`:
    - `bulkDeleteCustomersAction(ids: string[])`: validate all IDs are UUIDs via zod, `db.delete(schema.customer).where(inArray(schema.customer.id, ids)).returning({ id: schema.customer.id })`, audit log with BULK_DELETE action and metadata `{ deletedIds, count }`, revalidatePath('/dashboard/customers')
    - `importCustomersAction(customers: { firstName: string; lastName: string; email?: string; phone?: string; ... }[])`: validate with zod array schema, insert each customer via `db.insert(schema.customer).values(...)`, skip rows with duplicate emails if flagged, audit log with IMPORT action, revalidatePath('/dashboard/customers'), return `{ imported: number, skipped: number }`
    - `checkDuplicateEmailsAction(emails: string[])`: query existing customers by email using `inArray(schema.customer.email, emails)`, return array of emails that already exist

    **5. Add DAL helper** to `src/lib/dal/customers.ts`:
    - `checkDuplicateEmails(emails: string[])`: select emails from customer table where email in provided list, return string array of existing emails
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | head -30 && echo "--- Structural checks ---" && grep -c "export function InlineEdit\|export default function InlineEdit" src/components/dashboard/inline-edit.tsx && grep -c "export function BulkActionToolbar\|export default function BulkActionToolbar" src/components/dashboard/bulk-action-toolbar.tsx && grep -c "export function CsvImportDialog\|export default function CsvImportDialog" src/components/dashboard/csv-import-dialog.tsx && grep -c "bulkDeleteCustomersAction\|importCustomersAction\|checkDuplicateEmailsAction" src/lib/actions/customer-actions.ts && grep -c "checkDuplicateEmails" src/lib/dal/customers.ts && echo "ALL STRUCTURAL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - InlineEdit component renders value in display mode, switches to input on click, saves on confirm, cancels on X
    - BulkActionToolbar shows selected count with Export CSV and Delete buttons, Delete shows AlertDialog
    - CsvImportDialog parses CSV, shows preview, checks duplicates, imports via server action
    - bulkDeleteCustomersAction deletes multiple customers in one query with audit logging
    - importCustomersAction inserts customers with duplicate skip, returns imported/skipped count
    - All components use 'use client' directive and Shadcn UI primitives
  </acceptance_criteria>
  <done>Three reusable components exported, bulk delete/import/duplicate-check actions working, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Customer list bulk operations + CSV import wiring</name>
  <files>
    src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
  </files>
  <read_first>
    src/app/(dashboard)/dashboard/customers/customer-list-client.tsx,
    src/components/dashboard/bulk-action-toolbar.tsx,
    src/components/dashboard/csv-import-dialog.tsx,
    src/lib/utils/csv-export.ts
  </read_first>
  <action>
    Enhance `customer-list-client.tsx` to add bulk operations (per D-01, D-02):

    **1. Enable row selection in DataTable:**
    - Add a checkbox column as the first column using `enableRowSelection: true` on the ResponsiveDataTable
    - Add a select-all checkbox in the header column, individual checkboxes per row
    - Track selection state via `rowSelection` state variable and `onRowSelectionChange`

    **2. Wire BulkActionToolbar:**
    - Import and render `BulkActionToolbar` when `Object.keys(rowSelection).length > 0`
    - Export handler: get selected rows, map to flat objects `{ firstName, lastName, email, phone, createdAt }`, call `exportToCsv('customers.csv', rows)`
    - Delete handler: collect selected customer IDs, call `bulkDeleteCustomersAction(ids)`, show toast.promise, clear selection on success, invalidate customers query
    - Pass `onClearSelection` to reset `rowSelection` state to `{}`

    **3. Add CSV Import button:**
    - Add "Import CSV" button next to the existing "Add Customer" button in the header
    - Wrap it with `CsvImportDialog` -- on successful import, invalidate customers query and show success toast

    **4. Preserve existing functionality:**
    - Keep all existing columns, search, pagination, sorting, add/edit/delete dialogs
    - The bulk toolbar floats at the bottom and does not interfere with existing UI
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | head -30 && echo "--- Structural checks ---" && grep -c "BulkActionToolbar" src/app/\(dashboard\)/dashboard/customers/customer-list-client.tsx && grep -c "CsvImportDialog" src/app/\(dashboard\)/dashboard/customers/customer-list-client.tsx && grep -c "rowSelection" src/app/\(dashboard\)/dashboard/customers/customer-list-client.tsx && grep -c "bulkDeleteCustomersAction" src/app/\(dashboard\)/dashboard/customers/customer-list-client.tsx && echo "ALL STRUCTURAL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Checkbox column appears as first column in customer table
    - Selecting rows shows floating BulkActionToolbar at bottom with count, Export CSV, Delete buttons
    - Export CSV downloads a file with selected customer data
    - Bulk delete shows AlertDialog, deletes on confirm, clears selection, refreshes list
    - Import CSV button opens dialog, parses file, shows preview with duplicate detection, imports new customers
    - All existing customer list functionality (search, pagination, add, edit, delete) still works
  </acceptance_criteria>
  <done>Customer list supports bulk select/delete/export and CSV import with duplicate detection per FEAT-02</done>
</task>

<task type="auto">
  <name>Task 3: Customer detail inline editing + communication timeline + portal indicator</name>
  <files>
    src/app/(dashboard)/dashboard/customers/[id]/page.tsx,
    src/app/(dashboard)/dashboard/customers/[id]/customer-detail-client.tsx,
    src/lib/dal/customers.ts
  </files>
  <read_first>
    src/app/(dashboard)/dashboard/customers/[id]/page.tsx,
    src/lib/dal/customers.ts,
    src/lib/actions/customer-actions.ts,
    src/components/dashboard/inline-edit.tsx,
    src/lib/db/schema.ts (lines 115-147 for customer, 524-530 for relations)
  </read_first>
  <action>
    Enhance customer detail page with inline editing, communication timeline, quick-create actions, and portal indicator (per D-03):

    **1. Update DAL** -- add to `src/lib/dal/customers.ts`:
    - `getCustomerTimeline(customerId: string)`: query appointments for this customer (ordered by scheduledDate desc, limit 20) + query contacts by matching customer email (ordered by createdAt desc, limit 20). Merge into unified timeline array sorted by date. Each entry: `{ type: 'appointment' | 'contact', date: Date, summary: string, status: string, id: string }`.
    - Update `getCustomerWithDetails` to also include the customer's `userId` field (already returned) and load the `user` relation to check if a portal account exists.

    **2. Create customer-detail-client.tsx** at `src/app/(dashboard)/dashboard/customers/[id]/customer-detail-client.tsx`:
    - 'use client' component receiving `customer` and `timeline` as props
    - **Contact Info card** with InlineEdit for each field (firstName, lastName, email, phone, address fields). Each `onSave` calls `updateCustomerAction` with a FormData containing only the changed field + customer id, shows toast.promise, refreshes via router.refresh().
    - **Portal Account indicator**: If `customer.user` exists, show a green Badge "Portal Linked" with user email. If not, show a gray Badge "No Portal Account".
    - **Quick Actions section**: "New Appointment" and "New Session" buttons that link to `/dashboard/appointments?customerId={id}` and `/dashboard/sessions?customerId={id}` respectively (pre-fill customer in the create dialog).
    - **Communication Timeline card**: render the merged timeline entries in reverse chronological order. Appointments show as blue dot with date, type, status badge. Contacts show as green dot with date, subject, status badge. Use a simple vertical timeline layout with connecting line.
    - Keep the existing Medical Info, Appointment History, and Session History cards, but use InlineEdit for the notes field and medical arrays.

    **3. Update the server page** `src/app/(dashboard)/dashboard/customers/[id]/page.tsx`:
    - Fetch `getCustomerTimeline(id)` alongside `getCustomerWithDetails(id)`
    - Pass both to `CustomerDetailClient` component
    - Keep breadcrumbs and header in the server component, move card content to client component
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | head -30 && echo "--- Structural checks ---" && grep -c "export function CustomerDetailClient\|export default function CustomerDetailClient" src/app/\(dashboard\)/dashboard/customers/\[id\]/customer-detail-client.tsx && grep -c "getCustomerTimeline" src/lib/dal/customers.ts && grep -c "InlineEdit" src/app/\(dashboard\)/dashboard/customers/\[id\]/customer-detail-client.tsx && grep -c "Portal" src/app/\(dashboard\)/dashboard/customers/\[id\]/customer-detail-client.tsx && echo "ALL STRUCTURAL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Clicking any text field on customer detail (name, email, phone, address, notes) enters inline edit mode with save/cancel
    - Saving an inline edit calls updateCustomerAction and refreshes the page data
    - Portal account indicator shows "Portal Linked" or "No Portal Account"
    - "New Appointment" and "New Session" buttons link to respective pages with customer pre-selected
    - Communication timeline shows appointments and contact submissions in chronological order
    - Medical info and existing appointment/session history tables are preserved
  </acceptance_criteria>
  <done>Customer detail page has full inline editing, communication timeline, quick-create buttons, and portal indicator per FEAT-03</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Customer list page renders with checkbox column, bulk toolbar appears on selection
- Customer detail page renders with inline editing on all text fields
- New papaparse dependency installed and importable
</verification>

<success_criteria>
- FEAT-02: Artist can bulk select, delete, export customers to CSV, and import from CSV with duplicate detection
- FEAT-03: Artist can inline-edit customer fields, see portal link status, create appointments/sessions from customer page, view communication timeline
- InlineEdit, BulkActionToolbar, CsvImportDialog components are reusable for other pages
</success_criteria>

<output>
After completion, create `.planning/phases/18-feature-depth-records/18-01-SUMMARY.md`
</output>
