---
phase: 18-feature-depth-records
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
  - src/lib/actions/appointment-actions.ts
  - src/app/(dashboard)/dashboard/sessions/[id]/page.tsx
  - src/app/(dashboard)/dashboard/sessions/[id]/session-detail-client.tsx
  - src/app/(dashboard)/dashboard/sessions/session-list-client.tsx
  - src/lib/dal/sessions.ts
  - src/lib/actions/session-actions.ts
autonomous: true
requirements: [FEAT-04, FEAT-05]

must_haves:
  truths:
    - "Creating or editing an appointment that overlaps an existing one shows a conflict warning AlertDialog before saving"
    - "Artist can override the conflict warning and save anyway with forceOverride"
    - "Artist can click into a session from the list to see a full detail page with all fields"
    - "Session detail page shows linked payment records for that session"
    - "Artist can click any text field on session detail to edit it inline with save/cancel"
    - "Session detail page shows reference image gallery with ability to add images"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/sessions/[id]/page.tsx"
      provides: "Server page fetching session with payments for detail view"
      exports: ["default"]
    - path: "src/app/(dashboard)/dashboard/sessions/[id]/session-detail-client.tsx"
      provides: "Client component with inline editing, payments table, image gallery"
      exports: ["SessionDetailClient"]
    - path: "src/lib/actions/appointment-actions.ts"
      provides: "Updated appointment actions with conflict override support"
      exports: ["createAppointmentAction", "updateAppointmentAction"]
  key_links:
    - from: "src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx"
      to: "src/lib/actions/appointment-actions.ts"
      via: "Catches conflict error, shows AlertDialog, re-submits with forceOverride"
      pattern: "forceOverride|scheduling conflict"
    - from: "src/app/(dashboard)/dashboard/sessions/[id]/session-detail-client.tsx"
      to: "src/components/dashboard/inline-edit.tsx"
      via: "Uses InlineEdit from Plan 01 for per-field editing"
      pattern: "InlineEdit"
    - from: "src/app/(dashboard)/dashboard/sessions/[id]/session-detail-client.tsx"
      to: "src/lib/dal/payments.ts"
      via: "getPaymentsBySession for linked payment records"
      pattern: "getPaymentsBySession"
---

<objective>
Add appointment conflict detection UI and a full session detail page with inline editing, linked payments, and image gallery.

Purpose: FEAT-04 surfaces existing backend conflict checks as user-facing warnings instead of hard errors. FEAT-05 gives the artist a comprehensive session view with quick editing, financial overview, and reference image management.
Output: Conflict override flow on appointment forms, new session detail page at /dashboard/sessions/[id].
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-feature-depth-records/18-RESEARCH.md
@.planning/phases/18-feature-depth-records/18-01-SUMMARY.md

@src/lib/actions/appointment-actions.ts
@src/lib/dal/appointments.ts
@src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
@src/lib/dal/sessions.ts
@src/lib/dal/payments.ts
@src/lib/actions/session-actions.ts
@src/app/(dashboard)/dashboard/sessions/page.tsx
@src/app/(dashboard)/dashboard/sessions/session-list-client.tsx
@src/components/dashboard/inline-edit.tsx
@src/components/dashboard/media-uploader.tsx
@src/lib/db/schema.ts
@src/app/api/upload/token/route.ts

<interfaces>
From src/lib/actions/appointment-actions.ts:
```typescript
export async function createAppointmentAction(formData: FormData): Promise<ActionResult<{ id: string }>>;
export async function updateAppointmentAction(id: string, formData: FormData): Promise<ActionResult<{ id: string }>>;
export async function deleteAppointmentAction(id: string): Promise<ActionResult<void>>;
```

From src/lib/dal/appointments.ts:
```typescript
export async function checkSchedulingConflict(proposedDate: Date, durationHours?: number): Promise<boolean>;
```

From src/lib/dal/sessions.ts:
```typescript
export const getSessions = cache(async (opts) => ...);
export const getSessionById = cache(async (id: string) => ...);
export async function createSession(data: CreateSessionData) { ... }
export async function updateSession(id: string, data: Partial<CreateSessionData>) { ... }
export async function deleteSession(id: string) { ... }
```

From src/lib/dal/payments.ts:
```typescript
export const getPaymentsBySession = cache((tattooSessionId: string) => ...);
```

tattooSession schema fields: id, customerId, artistId, appointmentId, appointmentDate, duration, status, designDescription, placement, size, style, referenceImages (text array), hourlyRate, estimatedHours, depositAmount, totalCost, paidAmount, notes, aftercareProvided, consentSigned, consentSignedAt, consentSignedBy, createdAt, updatedAt.

tattooSession relations: customer, artist, appointment, payments (many).

From src/components/dashboard/inline-edit.tsx (created in Plan 01):
```typescript
export function InlineEdit({ value, onSave, label, type, options }: InlineEditProps);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Appointment conflict override UI</name>
  <files>
    src/lib/actions/appointment-actions.ts,
    src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
  </files>
  <read_first>
    src/lib/actions/appointment-actions.ts,
    src/lib/dal/appointments.ts (lines 155-176 for checkSchedulingConflict),
    src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
  </read_first>
  <action>
    Surface scheduling conflict warnings in the appointment UI (per D-04):

    **1. Update appointment actions** in `src/lib/actions/appointment-actions.ts`:
    - Change `createAppointmentAction` signature: accept an optional `options?: { forceOverride?: boolean }` parameter after formData
    - Instead of `throw new Error('Scheduling conflict...')`, return `{ success: false, error: 'SCHEDULING_CONFLICT' }` as a recognizable error code when conflict detected and `forceOverride` is not true
    - When `forceOverride` is true, skip the conflict check entirely and proceed with creation
    - Apply same pattern to `updateAppointmentAction`: return `{ success: false, error: 'SCHEDULING_CONFLICT' }` instead of throwing, and accept `options?: { forceOverride?: boolean }`
    - Keep all other validation, audit logging, and revalidatePath behavior unchanged

    **2. Update appointment-list-client.tsx** to handle conflict flow:
    - Add a `conflictDialogOpen` state and a `pendingFormData` ref to hold form data when conflict detected
    - When the create/edit action returns `error === 'SCHEDULING_CONFLICT'`:
      - Store the form data in `pendingFormData`
      - Open a conflict warning AlertDialog: "Another appointment exists at this time. Do you want to schedule anyway?"
      - "Cancel" closes dialog and clears pendingFormData
      - "Schedule Anyway" re-calls the action with `{ forceOverride: true }` using the stored form data
    - Show the conflict AlertDialog using the existing AlertDialog components already imported in the file
    - After successful override, close dialog, clear pendingFormData, show success toast, close the create/edit dialog
    - Keep all existing functionality (search, pagination, status filters, notes display) intact
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | head -30 && echo "--- Structural checks ---" && grep -c "SCHEDULING_CONFLICT" src/lib/actions/appointment-actions.ts && grep -c "forceOverride" src/lib/actions/appointment-actions.ts && grep -c "forceOverride\|conflictDialog\|SCHEDULING_CONFLICT" src/app/\(dashboard\)/dashboard/appointments/appointment-list-client.tsx && echo "ALL STRUCTURAL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Appointment actions return SCHEDULING_CONFLICT error code instead of throwing
    - Conflict detected on create/edit shows AlertDialog with warning message
    - "Schedule Anyway" re-submits with forceOverride, successfully creates/updates appointment
    - "Cancel" closes dialog without creating appointment
    - No infinite loop -- forceOverride flag prevents re-checking conflict
    - All existing appointment list functionality preserved
  </acceptance_criteria>
  <done>Appointment create/edit surfaces conflict warnings via AlertDialog with override option per FEAT-04</done>
</task>

<task type="auto">
  <name>Task 2: Session detail page with inline editing, linked payments, and image gallery</name>
  <files>
    src/app/(dashboard)/dashboard/sessions/[id]/page.tsx,
    src/app/(dashboard)/dashboard/sessions/[id]/session-detail-client.tsx,
    src/app/(dashboard)/dashboard/sessions/session-list-client.tsx,
    src/lib/dal/sessions.ts,
    src/lib/actions/session-actions.ts
  </files>
  <read_first>
    src/lib/dal/sessions.ts,
    src/lib/dal/payments.ts (lines 127-145 for getPaymentsBySession),
    src/lib/actions/session-actions.ts,
    src/app/(dashboard)/dashboard/sessions/session-list-client.tsx,
    src/components/dashboard/inline-edit.tsx,
    src/components/dashboard/media-uploader.tsx,
    src/app/api/upload/token/route.ts,
    src/lib/db/schema.ts (lines 204-238 for tattooSession, 330-351 for payment, 544-550 for relations)
  </read_first>
  <action>
    Create full session detail page with inline editing, linked payments, and image gallery (per D-05, D-06):

    **1. Update session DAL** in `src/lib/dal/sessions.ts`:
    - Add `getSessionWithDetails(id: string)`: use `db.query.tattooSession.findFirst` with `with: { customer: true, artist: true, appointment: true, payments: { with: { customer: true } } }` -- returns full session with related records. Use cache wrapper.
    - IMPORTANT: Use variable name `tattooSessionData` (not `session`) to avoid collision with auth session. Reference Research pitfall 6.

    **2. Update session actions** in `src/lib/actions/session-actions.ts`:
    - Add `updateSessionFieldAction(id: string, field: string, value: string)`: validate field name against allowed list (designDescription, placement, size, style, notes, duration, hourlyRate, estimatedHours), call `updateSession(id, { [field]: value })` with appropriate type coercion (number fields parsed via Number()), audit log with INLINE_EDIT action and `{ field, newValue }` metadata, revalidatePath(`/dashboard/sessions/${id}`), return ActionResult
    - Add `addSessionImageAction(id: string, imageUrl: string)`: fetch current session, append imageUrl to referenceImages array, update via `updateSession(id, { referenceImages: [...existing, imageUrl] })`, revalidatePath, return ActionResult
    - Add `removeSessionImageAction(id: string, imageUrl: string)`: fetch current session, filter out imageUrl from referenceImages, update, revalidatePath, return ActionResult

    **3. Create server page** at `src/app/(dashboard)/dashboard/sessions/[id]/page.tsx`:
    - `await connection()` for Neon
    - `const { id } = await params`
    - Fetch `getSessionWithDetails(id)` -- if null, call `notFound()`
    - Breadcrumb: Sessions > Session #id.slice(0,8)
    - Render `<SessionDetailClient session={sessionData} />`

    **4. Create SessionDetailClient** at `src/app/(dashboard)/dashboard/sessions/[id]/session-detail-client.tsx`:
    - 'use client' component receiving the full session object with customer, artist, appointment, payments
    - **Session Info Card**: Show status badge, customer name (link to customer detail), artist name, appointment date. Use InlineEdit for: designDescription, placement, size, style, notes, duration, hourlyRate, estimatedHours. Each onSave calls `updateSessionFieldAction`. Use toast.promise for loading/success/error.
    - **Financial Summary Card**: Show totalCost, depositAmount, paidAmount, remaining balance (totalCost - paidAmount). Display as read-only formatted currency.
    - **Linked Payments Card**: Table showing related payments from `session.payments` -- columns: date, type (StatusBadge), amount (formatted currency), status (StatusBadge). If no payments, show empty state "No payments recorded".
    - **Reference Images Card**: Grid of existing images from `session.referenceImages` array. Each image has a delete button (X icon, triggers removeSessionImageAction with AlertDialog confirmation). Add button opens the media uploader (reuse MediaUploader component pattern). On successful upload, call addSessionImageAction with the blob URL. If no images, show empty state with upload prompt.
    - **Metadata footer**: Show createdAt, updatedAt, aftercareProvided badge, consentSigned badge.

    **5. Add link from session list** in `session-list-client.tsx`:
    - In the actions dropdown (MoreHorizontal menu), add a "View Details" link item that navigates to `/dashboard/sessions/${session.id}`
    - Also make the session design description text clickable as a link to the detail page
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && npx tsc --noEmit 2>&1 | head -30 && echo "--- Structural checks ---" && grep -c "getSessionWithDetails" src/lib/dal/sessions.ts && grep -c "export function SessionDetailClient\|export default function SessionDetailClient" src/app/\(dashboard\)/dashboard/sessions/\[id\]/session-detail-client.tsx && grep -c "InlineEdit" src/app/\(dashboard\)/dashboard/sessions/\[id\]/session-detail-client.tsx && grep -c "updateSessionFieldAction\|addSessionImageAction\|removeSessionImageAction" src/lib/actions/session-actions.ts && grep -c "View Details\|sessions/" src/app/\(dashboard\)/dashboard/sessions/session-list-client.tsx && echo "ALL STRUCTURAL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Navigating to /dashboard/sessions/[id] shows full session detail page
    - Session info card shows all fields with InlineEdit -- clicking a value opens edit mode, save updates the field
    - Financial summary card shows totalCost, depositAmount, paidAmount, remaining balance
    - Linked payments card lists all payments for this session with date, type, amount, status
    - Reference images section shows existing images in a grid, allows adding via upload and removing with confirmation
    - Session list has "View Details" link in actions menu
    - No variable name collision with auth "session" -- uses tattooSessionData/sessionData naming
  </acceptance_criteria>
  <done>Session detail page at /dashboard/sessions/[id] with inline editing, linked payments, and image gallery per FEAT-05</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Appointment conflict on overlapping time shows AlertDialog warning
- Session detail page renders at /dashboard/sessions/[id] with all cards
- InlineEdit works on session fields and saves to database
</verification>

<success_criteria>
- FEAT-04: Appointment conflict detection surfaces as a UI warning with override option, not a hard error
- FEAT-05: Session detail page shows full info with inline editing, linked payments, and reference image gallery
</success_criteria>

<output>
After completion, create `.planning/phases/18-feature-depth-records/18-02-SUMMARY.md`
</output>
