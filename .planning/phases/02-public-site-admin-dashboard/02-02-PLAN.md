---
phase: 02-public-site-admin-dashboard
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(public)/about/page.tsx
  - src/app/(public)/faq/page.tsx
  - src/components/public/public-nav.tsx
  - src/components/public/public-footer.tsx
  - src/components/public/hero-section.tsx
  - src/components/public/mobile-nav.tsx
  - src/app/globals.css
autonomous: true
requirements: [PUB-01, PUB-06, PUB-08]

must_haves:
  truths:
    - "Home page renders hero with headline, subline, and brand-accent CTA button"
    - "PublicNav is sticky with logo, nav links, and CTA button"
    - "PublicNav becomes hamburger menu below 768px"
    - "About page renders studio information"
    - "FAQ page renders accordion with expandable questions"
    - "PublicFooter renders 3-column grid on desktop, stacked on mobile"
  artifacts:
    - path: "src/components/public/public-nav.tsx"
      provides: "Sticky responsive navigation"
      contains: "Book a Consultation"
    - path: "src/components/public/hero-section.tsx"
      provides: "Full-width hero with CTA"
      contains: "Custom Tattoo Art by Fernando Govea"
    - path: "src/app/(public)/page.tsx"
      provides: "Home page with hero and sections"
      min_lines: 30
    - path: "src/app/(public)/faq/page.tsx"
      provides: "FAQ page with accordion"
      contains: "Accordion"
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/public/public-nav.tsx"
      via: "import and render"
      pattern: "import.*PublicNav"
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/public/public-footer.tsx"
      via: "import and render"
      pattern: "import.*PublicFooter"
---

<objective>
Build the public site layout (navigation + footer) and core static pages (home, about, FAQ) with mobile-responsive design.

Purpose: Establishes the public site shell that all other public pages render within, and delivers three of the simplest pages that have no data dependencies.
Output: Working public site with nav, footer, home page with hero, about page, and FAQ with accordion.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-public-site-admin-dashboard/02-CONTEXT.md
@.planning/phases/02-public-site-admin-dashboard/02-UI-SPEC.md
@.planning/phases/02-public-site-admin-dashboard/02-RESEARCH.md

<interfaces>
<!-- Phase 1 existing components -->
From src/components/ui/button.tsx:
```typescript
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link';
  size?: 'default' | 'sm' | 'lg' | 'icon';
  asChild?: boolean;
}
export const Button: React.FC<ButtonProps>;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

<!-- shadcn components installed in 02-01 -->
From src/components/ui/accordion.tsx: AccordionItem, AccordionTrigger, AccordionContent
From src/components/ui/sheet.tsx: Sheet, SheetTrigger, SheetContent
From src/components/ui/separator.tsx: Separator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public layout with navigation and footer</name>
  <files>src/app/(public)/layout.tsx, src/components/public/public-nav.tsx, src/components/public/public-footer.tsx, src/components/public/mobile-nav.tsx, src/app/globals.css</files>
  <read_first>
    - /Users/richard/Developer/ink37-tattoos/src/app/(public)/layout.tsx
    - /Users/richard/Developer/ink37-tattoos/src/app/globals.css
    - /Users/richard/Developer/ink37-tattoos/src/components/ui/button.tsx
    - /Users/richard/Developer/ink37-tattoos/src/components/ui/sheet.tsx
    - /Users/richard/Developer/ink37-tattoos/src/lib/utils.ts
    - /Users/richard/Developer/ink37-tattoos/.planning/phases/02-public-site-admin-dashboard/02-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Add brand accent CSS variable to globals.css**
In the existing globals.css `@theme` block (or `:root` if that's the pattern), add:
```css
--brand-accent: oklch(0.637 0.237 25.331);
--brand-accent-foreground: #ffffff;
```

**Step 2: Create PublicNav component** at `src/components/public/public-nav.tsx`
'use client' component (needs useState for mobile menu).

Structure:
- Sticky header: `fixed top-0 w-full h-16 z-50 bg-white/95 backdrop-blur border-b`
- Left: Logo text "INK 37" linking to "/" (font-semibold text-xl)
- Center (hidden on mobile, flex on md+): nav links -- "Gallery" (/gallery), "Services" (/services), "About" (/about), "FAQ" (/faq), "Contact" (/contact)
- Right (hidden on mobile, flex on md+): `<Button>` with brand-accent background "Book a Consultation" linking to /booking
- Right (md:hidden): Hamburger icon button that opens MobileNav sheet

Nav links use `next/link` with `pathname` comparison for active state (font-semibold when active).

**Step 3: Create MobileNav component** at `src/components/public/mobile-nav.tsx`
'use client' component using shadcn Sheet component.

Structure:
- Sheet opens from right side
- SheetTrigger is the hamburger Menu icon (lucide-react)
- SheetContent contains vertical nav links (same as desktop) with py-4 spacing
- Bottom of sheet: "Book a Consultation" button (full width, brand-accent)
- Each link has min-height 44px for touch targets (per UI spec)
- Sheet closes on link click (use SheetClose or controlled state)

**Step 4: Create PublicFooter component** at `src/components/public/public-footer.tsx`
Server component (no interactivity needed).

Structure:
- Full-width bg-neutral-900 text-white py-12
- Max-w-7xl mx-auto px-4 md:px-6
- 3-column grid on lg (grid-cols-3), stacked on mobile
- Column 1: "INK 37 TATTOOS" heading, brief studio description, copyright year
- Column 2: "Quick Links" heading, links to Gallery, Services, Booking, Contact
- Column 3: "Contact" heading, studio address placeholder, email, phone placeholder, social media icon links (Instagram, Facebook) using lucide-react icons
- Footer links are text-neutral-400 hover:text-white transition
- Social icons hover uses brand-accent color

**Step 5: Update public layout** at `src/app/(public)/layout.tsx`
Import PublicNav and PublicFooter. Structure:
```tsx
export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <PublicNav />
      <main className="pt-16 min-h-screen">{children}</main>
      <PublicFooter />
    </>
  );
}
```
The `pt-16` offsets content below the fixed nav.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/public/public-nav.tsx contains "Book a Consultation" and "fixed" and "z-50"
    - src/components/public/public-nav.tsx contains links to "/gallery", "/services", "/about", "/faq", "/contact", "/booking"
    - src/components/public/mobile-nav.tsx contains "Sheet" and "Menu" (hamburger icon)
    - src/components/public/mobile-nav.tsx contains "44px" or "min-h-[44px]" or "h-11" for touch targets
    - src/components/public/public-footer.tsx contains "INK 37" and "grid" and "Quick Links"
    - src/app/(public)/layout.tsx contains "import" and "PublicNav" and "PublicFooter"
    - src/app/(public)/layout.tsx contains "pt-16" for nav offset
    - src/app/globals.css contains "--brand-accent"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Public layout renders sticky nav with logo/links/CTA, hamburger menu on mobile using Sheet, and 3-column footer. All public pages will inherit this layout.</done>
</task>

<task type="auto">
  <name>Task 2: Create home page, about page, and FAQ page</name>
  <files>src/app/(public)/page.tsx, src/components/public/hero-section.tsx, src/app/(public)/about/page.tsx, src/app/(public)/faq/page.tsx</files>
  <read_first>
    - /Users/richard/Developer/ink37-tattoos/src/app/(public)/page.tsx
    - /Users/richard/Developer/ink37-tattoos/.planning/phases/02-public-site-admin-dashboard/02-UI-SPEC.md
    - /Users/richard/Developer/ink37-tattoos/.planning/phases/02-public-site-admin-dashboard/02-CONTEXT.md
    - /Users/richard/Developer/ink37-tattoos/src/components/ui/accordion.tsx
  </read_first>
  <action>
**Step 1: Create HeroSection component** at `src/components/public/hero-section.tsx`
'use client' component (for Framer Motion entrance animation).

Structure:
- Full-bleed section with dark background (bg-neutral-950) and min-h-[70vh] or similar
- Content centered vertically and horizontally, max-w-4xl mx-auto px-4
- Headline (Display role): "Custom Tattoo Art by Fernando Govea" -- 40px (2.5rem) on desktop, 32px on mobile, font-semibold, text-white
- Subline: "Professional tattoo artistry in a clean, comfortable studio. Every piece is a collaboration." -- text-neutral-300, text-lg
- CTA button: "Book a Consultation" -- uses Button component with custom brand-accent styling (`bg-[--brand-accent] hover:bg-[--brand-accent]/90 text-white`), links to /booking
- Secondary CTA: "View Gallery" -- outline variant Button, text-white border-white, links to /gallery
- Framer Motion: fade-in + slide-up animation (400ms, easing [0.25, 0.1, 0.25, 1]) on the text content container. Import `motion` from 'framer-motion'. Use `initial={{ opacity: 0, y: 20 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.4, ease: [0.25, 0.1, 0.25, 1] }}`.

**Step 2: Update home page** at `src/app/(public)/page.tsx`
Server component that imports HeroSection. Export metadata for SEO.

Structure:
```typescript
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Ink 37 Tattoos | Custom Tattoo Art by Fernando Govea',
  description: 'Professional tattoo artistry in a clean, comfortable studio. Book a consultation for your custom tattoo design.',
  openGraph: {
    title: 'Ink 37 Tattoos | Custom Tattoo Art by Fernando Govea',
    description: 'Professional tattoo artistry in a clean, comfortable studio.',
    type: 'website',
  },
};
```

Page sections (separated by 48px vertical spacing):
1. HeroSection (full-bleed)
2. "Our Services" preview section -- 3 cards in a grid (Consultation, Design Review, Tattoo Session) each with icon (lucide), heading, brief description. Link to /services. Use Card component.
3. "Gallery Preview" section -- heading "Our Work", subtext "Browse our portfolio of custom tattoo art", CTA "View Full Gallery" linking to /gallery. Placeholder grid of 6 skeleton rectangles (will be replaced when gallery data exists).
4. "Ready for your next piece?" CTA section -- dark bg section with heading and "Book a Consultation" button

**Step 3: Create about page** at `src/app/(public)/about/page.tsx`
Server component.

Export metadata:
```typescript
export const metadata: Metadata = {
  title: 'About | Ink 37 Tattoos',
  description: 'Learn about Ink 37 Tattoos studio, our artist Fernando Govea, and our commitment to custom tattoo artistry.',
};
```

Structure:
- Page heading "About Ink 37 Tattoos" (Heading role, 24px)
- Studio description section -- paragraphs about the studio, artist background, philosophy
- "Our Process" section -- numbered steps (similar to ProcessSteps from UI spec): 1. Consultation, 2. Design, 3. Session, 4. Aftercare. Each step has a number badge, title, and description.
- CTA section linking to /booking

Content should be realistic for a tattoo studio but can be placeholder-ish -- the owner can customize later.

**Step 4: Create FAQ page** at `src/app/(public)/faq/page.tsx`
Server component using shadcn Accordion.

Export metadata:
```typescript
export const metadata: Metadata = {
  title: 'FAQ | Ink 37 Tattoos',
  description: 'Frequently asked questions about tattoo appointments, pricing, aftercare, and studio policies at Ink 37 Tattoos.',
};
```

Structure:
- Page heading "Frequently Asked Questions"
- Accordion with `type="single"` `collapsible` prop
- FAQ items (8-10 questions covering common tattoo studio FAQs):
  1. "How do I book an appointment?" -- Explain consultation process, link to /booking
  2. "How much does a tattoo cost?" -- Explain pricing varies by size/complexity/time
  3. "How should I prepare for my tattoo session?" -- Pre-session tips
  4. "What is the aftercare process?" -- Aftercare instructions
  5. "Do you do cover-ups?" -- Yes, consultation required
  6. "What styles do you specialize in?" -- Reference styles from gallery filter list
  7. "How old do I have to be to get a tattoo?" -- 18+ with valid ID
  8. "What forms of payment do you accept?" -- Cash, credit/debit (Stripe coming soon)
  9. "Can I bring a friend?" -- Yes, limited space policy
  10. "What if I need to reschedule?" -- Cancellation/reschedule policy

- CTA section at bottom linking to /contact and /booking
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/public/hero-section.tsx contains "Custom Tattoo Art by Fernando Govea"
    - src/components/public/hero-section.tsx contains "Book a Consultation" and "View Gallery"
    - src/components/public/hero-section.tsx contains "framer-motion" or "motion"
    - src/components/public/hero-section.tsx contains "opacity: 0" and "opacity: 1"
    - src/app/(public)/page.tsx contains "metadata" and "Ink 37 Tattoos"
    - src/app/(public)/page.tsx contains "HeroSection"
    - src/app/(public)/page.tsx contains "Our Services" or "Services"
    - src/app/(public)/about/page.tsx contains "About Ink 37" and "metadata"
    - src/app/(public)/about/page.tsx contains "Consultation" and "Design" and "Aftercare"
    - src/app/(public)/faq/page.tsx contains "Accordion" and "FAQ"
    - src/app/(public)/faq/page.tsx contains "How do I book" and "How much does a tattoo cost"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Home page renders hero with headline/CTAs and service preview sections. About page shows studio info and process steps. FAQ page has accordion with 8-10 tattoo-related questions. All pages have SEO metadata.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Home page at / renders hero section with "Custom Tattoo Art by Fernando Govea"
- Navigation shows on all public pages with correct links
- FAQ accordion expands/collapses questions
- Mobile nav hamburger menu works below 768px
</verification>

<success_criteria>
Public site shell (nav + footer) is complete and responsive. Home page has hero with animated entrance, service preview, and gallery preview sections. About page shows studio info and process steps. FAQ page has working accordion. All pages have SEO metadata exported.
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-site-admin-dashboard/02-02-SUMMARY.md`
</output>
