---
phase: 24-monitoring-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - sentry.client.config.ts
  - sentry.server.config.ts
  - sentry.edge.config.ts
  - src/instrumentation.ts
  - src/app/global-error.tsx
  - src/lib/env.ts
  - src/app/api/health/route.ts
  - src/app/layout.tsx
autonomous: true
requirements: [MON-01, MON-02, MON-04]
user_setup:
  - service: sentry
    why: "Error tracking in production"
    env_vars:
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard -> Settings -> Auth Tokens -> Create New Token (org:read, project:read, project:write scopes)"
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Sentry Dashboard -> Project Settings -> Client Keys (DSN)"
      - name: SENTRY_ORG
        source: "Sentry Dashboard -> Settings -> General -> Organization Slug"
      - name: SENTRY_PROJECT
        source: "Sentry Dashboard -> Projects -> Project Slug"

must_haves:
  truths:
    - "An unhandled error in production is captured by Sentry with stack trace and context"
    - "GET /api/health returns 200 with db:connected when database is reachable"
    - "GET /api/health returns 503 with db:disconnected when database is unreachable"
    - "Web Vitals (LCP, FID, CLS) are tracked and reported for the public site"
  artifacts:
    - path: "sentry.client.config.ts"
      provides: "Client-side Sentry initialization"
      contains: "Sentry.init"
    - path: "sentry.server.config.ts"
      provides: "Server-side Sentry initialization"
      contains: "Sentry.init"
    - path: "sentry.edge.config.ts"
      provides: "Edge runtime Sentry initialization"
      contains: "Sentry.init"
    - path: "src/instrumentation.ts"
      provides: "Next.js instrumentation hook loading Sentry"
      contains: "register"
    - path: "src/app/global-error.tsx"
      provides: "Global error boundary reporting to Sentry"
      contains: "captureException"
    - path: "src/app/api/health/route.ts"
      provides: "Health check endpoint with DB connectivity"
      exports: ["GET"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with SpeedInsights component"
      contains: "SpeedInsights"
  key_links:
    - from: "next.config.ts"
      to: "@sentry/nextjs"
      via: "withSentryConfig wrapper"
      pattern: "withSentryConfig"
    - from: "src/instrumentation.ts"
      to: "sentry.server.config.ts"
      via: "dynamic import in register()"
      pattern: "import.*sentry\\.server\\.config"
    - from: "src/app/api/health/route.ts"
      to: "src/lib/db"
      via: "SQL query to verify connectivity"
      pattern: "sql.*SELECT 1"
---

<objective>
Set up Sentry error tracking, health check endpoint, and web vitals monitoring.

Purpose: Production observability -- errors are captured automatically, uptime can be monitored externally, and real user performance metrics are tracked.
Output: Sentry SDK integrated with Next.js, /api/health endpoint live, SpeedInsights reporting web vitals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next.config.ts
@src/lib/env.ts
@src/lib/db/index.ts
@src/app/layout.tsx
@src/components/providers.tsx
</context>

<interfaces>
<!-- Existing contracts the executor needs -->

From src/lib/db/index.ts:
```typescript
import { drizzle, type NeonDatabase } from 'drizzle-orm/neon-serverless';
import * as schema from './schema';

export function getDb(): NeonDatabase<typeof schema>;
export const db: NeonDatabase<typeof schema>;
export type Database = NeonDatabase<typeof schema>;
```

From src/lib/env.ts:
```typescript
import { z } from 'zod';

const envSchema = z.object({
  DATABASE_URL: z.url(),
  BETTER_AUTH_SECRET: z.string().min(32),
  // ... other vars
});

export function env(): Env;
```

From next.config.ts:
```typescript
const nextConfig: NextConfig = {
  output: 'standalone',
  async headers() { /* security headers */ },
};
export default nextConfig;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Sentry SDK and configure Next.js integration (MON-01)</name>
  <read_first>
    - next.config.ts (current config to wrap with withSentryConfig)
    - src/lib/env.ts (to add SENTRY env vars)
    - src/app/layout.tsx (to verify root layout structure)
  </read_first>
  <files>
    next.config.ts,
    sentry.client.config.ts,
    sentry.server.config.ts,
    sentry.edge.config.ts,
    src/instrumentation.ts,
    src/app/global-error.tsx,
    src/lib/env.ts
  </files>
  <action>
    1. Install @sentry/nextjs: `bun add @sentry/nextjs`

    2. Create `sentry.client.config.ts` in project root:
       - Import and call `Sentry.init()` with:
         - `dsn: process.env.NEXT_PUBLIC_SENTRY_DSN`
         - `tracesSampleRate: 0.1` (10% of transactions in production)
         - `replaysSessionSampleRate: 0` (disabled -- not needed)
         - `replaysOnErrorSampleRate: 0` (disabled -- not needed)
         - `environment: process.env.NODE_ENV`
         - `enabled: process.env.NODE_ENV === 'production'`
       - Do NOT enable Session Replay (adds bundle size with no benefit for this project)

    3. Create `sentry.server.config.ts` in project root:
       - Import and call `Sentry.init()` with:
         - `dsn: process.env.NEXT_PUBLIC_SENTRY_DSN`
         - `tracesSampleRate: 0.1`
         - `environment: process.env.NODE_ENV`
         - `enabled: process.env.NODE_ENV === 'production'`

    4. Create `sentry.edge.config.ts` in project root:
       - Same as server config but imported by edge runtime

    5. Create `src/instrumentation.ts`:
       - Export async `register()` function
       - Inside, check `process.env.NEXT_RUNTIME`:
         - If 'nodejs': dynamically import '../sentry.server.config'
         - If 'edge': dynamically import '../sentry.edge.config'
       - Export `onRequestError` from `@sentry/nextjs` for automatic error capture

    6. Create `src/app/global-error.tsx`:
       - 'use client' directive
       - Import `* as Sentry` from '@sentry/nextjs'
       - Call `Sentry.captureException(error)` in useEffect
       - Render a basic error UI with html/body tags (required for global-error)
       - Include a "Try again" button calling `reset()`
       - Note: global-error MUST include its own html/body tags since it replaces root layout

    7. Update `next.config.ts`:
       - Import `withSentryConfig` from '@sentry/nextjs'
       - Wrap the existing `nextConfig` export: `export default withSentryConfig(nextConfig, { ... })`
       - Sentry build options:
         - `org: process.env.SENTRY_ORG`
         - `project: process.env.SENTRY_PROJECT`
         - `silent: !process.env.CI` (suppress logs in dev)
         - `widenClientFileUpload: true` (better stack traces)
         - `disableLogger: true` (remove Sentry logger statements from client bundle)
         - `tunnelRoute: '/monitoring'` (avoid ad blockers in production)
         - `hideSourceMaps: true` (don't expose source maps publicly)
         - `automaticVercelMonitors: true`
       - IMPORTANT: Keep the existing securityHeaders array and headers() function intact
       - Update CSP connect-src to include `https://*.ingest.sentry.io` for Sentry event ingestion

    8. Update `src/lib/env.ts`:
       - Add to envSchema: `NEXT_PUBLIC_SENTRY_DSN: z.string().url().optional()` (optional so dev works without Sentry)
       - Add: `SENTRY_ORG: z.string().optional()`
       - Add: `SENTRY_PROJECT: z.string().optional()`
       - Note: SENTRY_AUTH_TOKEN is used at build time only (by Sentry webpack plugin), not at runtime -- do NOT add it to envSchema
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && grep -q "withSentryConfig" next.config.ts && grep -q "Sentry.init" sentry.client.config.ts && grep -q "Sentry.init" sentry.server.config.ts && grep -q "Sentry.init" sentry.edge.config.ts && grep -q "register" src/instrumentation.ts && grep -q "captureException" src/app/global-error.tsx && grep -q "NEXT_PUBLIC_SENTRY_DSN" src/lib/env.ts && echo "PASS: All Sentry files present with expected content"</automated>
  </verify>
  <acceptance_criteria>
    - @sentry/nextjs is in package.json dependencies
    - sentry.client.config.ts, sentry.server.config.ts, sentry.edge.config.ts exist in project root
    - src/instrumentation.ts exports register() with runtime-conditional imports
    - src/app/global-error.tsx captures exceptions to Sentry
    - next.config.ts wraps export with withSentryConfig
    - CSP connect-src includes sentry.io domain
    - env.ts includes NEXT_PUBLIC_SENTRY_DSN as optional
    - `bun run build` completes without errors (may warn about missing SENTRY_AUTH_TOKEN -- that is expected)
  </acceptance_criteria>
  <done>Sentry SDK fully integrated: client, server, and edge configs created; instrumentation hook registered; global error boundary captures unhandled errors; next.config wrapped with Sentry; env vars declared</done>
</task>

<task type="auto">
  <name>Task 2: Create health check endpoint with DB connectivity (MON-02)</name>
  <read_first>
    - src/lib/db/index.ts (to understand db import pattern)
    - src/app/api/webhooks/stripe/route.ts (to see existing route handler patterns)
  </read_first>
  <files>
    src/app/api/health/route.ts,
    src/__tests__/api-health.test.ts
  </files>
  <action>
    1. Create `src/app/api/health/route.ts`:
       - Import `sql` from 'drizzle-orm'
       - Import `db` from '@/lib/db'
       - Export GET handler (no auth required -- this is for external uptime monitors)
       - Try: Execute `await db.execute(sql\`SELECT 1\`)` to verify DB connectivity
       - On success: Return `NextResponse.json({ status: 'ok', db: 'connected', timestamp: new Date().toISOString() }, { status: 200 })`
       - On catch: Return `NextResponse.json({ status: 'error', db: 'disconnected', timestamp: new Date().toISOString() }, { status: 503 })`
       - Set `Cache-Control: no-store` header to prevent caching
       - Do NOT use the env() function -- health check should work even if env validation is incomplete (avoid circular failure)
       - Do NOT add rate limiting -- uptime monitors need reliable access
       - Set `export const dynamic = 'force-dynamic'` to prevent static generation
       - Set `export const runtime = 'nodejs'` (needs neon-serverless which uses ws)

    2. Create `src/__tests__/api-health.test.ts`:
       - Mock '@/lib/db' to control db.execute behavior
       - Test 1: When db.execute resolves, GET returns 200 with `{ status: 'ok', db: 'connected' }`
       - Test 2: When db.execute rejects, GET returns 503 with `{ status: 'error', db: 'disconnected' }`
       - Test 3: Response includes timestamp field
       - Test 4: Response has Cache-Control: no-store header
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && bun test src/__tests__/api-health.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/health/route.ts exports GET handler
    - Returns 200 + { status: 'ok', db: 'connected' } on healthy DB
    - Returns 503 + { status: 'error', db: 'disconnected' } on DB failure
    - Response includes timestamp
    - Response has no-cache headers
    - All 4 tests pass
  </acceptance_criteria>
  <done>Health check endpoint at /api/health returns 200 with DB connectivity status when healthy, 503 when database is unreachable, with test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Enable Vercel Speed Insights for web vitals tracking (MON-04)</name>
  <read_first>
    - src/app/layout.tsx (root layout where SpeedInsights component goes)
    - src/components/providers.tsx (to understand client component structure)
  </read_first>
  <files>
    src/app/layout.tsx
  </files>
  <action>
    1. Install @vercel/speed-insights: `bun add @vercel/speed-insights`

    2. Update `src/app/layout.tsx`:
       - Import `{ SpeedInsights }` from '@vercel/speed-insights/next'
       - Add `<SpeedInsights />` inside the body tag, after `<Providers>` (as a sibling, not inside Providers)
       - SpeedInsights is a server component in @vercel/speed-insights/next -- import from '/next' not the base package
       - This automatically tracks Core Web Vitals (LCP, FID/INP, CLS) when deployed to Vercel
       - No configuration needed -- it auto-detects the Vercel environment
       - In development, it will be a no-op (no data sent)

    3. Do NOT install @vercel/analytics separately -- Speed Insights covers web vitals tracking which is what MON-04 requires. Vercel Analytics (page views, visitors) is a separate product and not in scope.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && grep -q "SpeedInsights" src/app/layout.tsx && grep -q "@vercel/speed-insights" package.json && echo "PASS: SpeedInsights integrated in root layout"</automated>
  </verify>
  <acceptance_criteria>
    - @vercel/speed-insights is in package.json dependencies
    - SpeedInsights component rendered in root layout
    - Import uses '@vercel/speed-insights/next' (the Next.js-specific entry point)
    - Build succeeds with SpeedInsights included
  </acceptance_criteria>
  <done>Vercel Speed Insights enabled in root layout, automatically tracking LCP, FID/INP, and CLS for all pages when deployed to Vercel</done>
</task>

</tasks>

<verification>
1. All Sentry config files exist: `ls sentry.*.config.ts src/instrumentation.ts src/app/global-error.tsx`
2. Health endpoint test passes: `bun test src/__tests__/api-health.test.ts`
3. SpeedInsights in layout: `grep "SpeedInsights" src/app/layout.tsx`
4. Build succeeds: `bun run build` (may warn about missing SENTRY_AUTH_TOKEN -- acceptable in dev)
5. CSP updated: `grep "sentry.io" next.config.ts`
</verification>

<success_criteria>
- Sentry SDK fully configured for client, server, and edge runtimes
- Global error boundary catches and reports unhandled errors
- Health check returns 200/503 based on DB connectivity with full test coverage
- Web Vitals tracking enabled via Vercel Speed Insights
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/24-monitoring-observability/24-01-SUMMARY.md`
</output>
