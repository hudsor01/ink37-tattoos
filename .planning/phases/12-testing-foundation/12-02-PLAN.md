---
phase: 12-testing-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/server-actions.test.ts
  - src/__tests__/upload-endpoint.test.ts
autonomous: true
requirements:
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "Server actions enforce auth by throwing 'Unauthorized' when getCurrentSession returns null"
    - "Server actions validate input via Zod schemas and throw on invalid data"
    - "Server actions call the correct DAL function with validated data"
    - "Portal actions return {success:false, error} instead of throwing for user-facing errors"
    - "Upload token endpoint rejects non-staff users with 400 error"
    - "Upload token endpoint accepts staff/manager/admin/super_admin roles"
  artifacts:
    - path: "src/__tests__/server-actions.test.ts"
      provides: "Integration tests for server actions with mocked auth + DAL"
      min_lines: 180
    - path: "src/__tests__/upload-endpoint.test.ts"
      provides: "Integration tests for /api/upload/token with auth gate"
      min_lines: 60
  key_links:
    - from: "src/__tests__/server-actions.test.ts"
      to: "src/lib/actions/customer-actions.ts"
      via: "dynamic import with mocked auth and DAL"
      pattern: "import.*customer-actions"
    - from: "src/__tests__/server-actions.test.ts"
      to: "src/lib/actions/portal-actions.ts"
      via: "dynamic import with mocked auth and DB"
      pattern: "import.*portal-actions"
    - from: "src/__tests__/upload-endpoint.test.ts"
      to: "src/app/api/upload/token/route.ts"
      via: "dynamic import with mocked auth and vercel blob"
      pattern: "import.*upload/token/route"
---

<objective>
Test server actions (admin CRUD + portal mutations) and the upload token endpoint for auth enforcement, input validation, and correct DAL delegation.

Purpose: Verify that auth guards, input validation, and DAL delegation work correctly across server actions, and that the upload endpoint properly restricts access to staff+ roles.

Output: 2 new test files covering server actions and upload endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/actions/customer-actions.ts
@src/lib/actions/appointment-actions.ts
@src/lib/actions/portal-actions.ts
@src/lib/actions/store-actions.ts
@src/app/api/upload/token/route.ts
@src/__tests__/contact-form.test.ts
@vitest.config.ts

<interfaces>
<!-- Server action patterns to test -->

From src/lib/actions/customer-actions.ts:
```typescript
export async function createCustomerAction(formData: FormData)   // throws 'Unauthorized', uses CreateCustomerSchema, calls createCustomer, revalidatePath
export async function updateCustomerAction(id: string, formData: FormData)   // throws 'Unauthorized', uses UpdateCustomerSchema, calls updateCustomer
export async function deleteCustomerAction(id: string)   // throws 'Unauthorized', calls deleteCustomer
```

From src/lib/actions/appointment-actions.ts:
```typescript
export async function createAppointmentAction(formData: FormData)   // throws 'Unauthorized', uses CreateAppointmentSchema, calls createAppointment
export async function updateAppointmentAction(id: string, formData: FormData)   // throws 'Unauthorized', uses UpdateAppointmentSchema
export async function deleteAppointmentAction(id: string)   // throws 'Unauthorized', calls deleteAppointment
```

From src/lib/actions/portal-actions.ts:
```typescript
export async function signConsentAction(formData: FormData)   // returns {success:false, error} on failure (not throw)
export async function updateProfileAction(formData: FormData)   // returns {success:false, error} on failure (not throw)
```

From src/app/api/upload/token/route.ts:
```typescript
const STAFF_ROLES = ['staff', 'manager', 'admin', 'super_admin'];
export async function POST(request: Request): Promise<NextResponse>  // checks session.user.role in STAFF_ROLES
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server action integration tests</name>
  <files>src/__tests__/server-actions.test.ts</files>
  <read_first>
    - src/lib/actions/customer-actions.ts (source -- createCustomerAction, updateCustomerAction, deleteCustomerAction)
    - src/lib/actions/appointment-actions.ts (source -- createAppointmentAction, updateAppointmentAction, deleteAppointmentAction)
    - src/lib/actions/portal-actions.ts (source -- signConsentAction, updateProfileAction -- returns {success,error} not throws)
    - src/__tests__/contact-form.test.ts (reference pattern for mocking auth, DAL, next/headers, revalidatePath)
    - src/lib/security/validation.ts (Zod schemas used by actions)
    - vitest.config.ts
  </read_first>
  <behavior>
    Customer action tests:
    - createCustomerAction throws 'Unauthorized' when getCurrentSession returns null
    - createCustomerAction calls createCustomer with validated data from FormData
    - createCustomerAction calls revalidatePath('/dashboard/customers')
    - deleteCustomerAction throws 'Unauthorized' when no session
    - deleteCustomerAction calls deleteCustomer with the given id

    Appointment action tests:
    - createAppointmentAction throws 'Unauthorized' when getCurrentSession returns null
    - createAppointmentAction calls createAppointment with validated schema data
    - deleteAppointmentAction calls deleteAppointment and revalidatePath

    Portal action tests:
    - signConsentAction returns {success:false, error:'You must be logged in...'} when no session
    - signConsentAction returns {success:false, error} when validation fails (missing sessionId)
    - signConsentAction returns {success:false, error:'No linked customer account found.'} when customer lookup returns null
    - signConsentAction returns {success:false, error:'Tattoo session not found.'} when session not found for customer
    - signConsentAction returns {success:false, error:'Consent has already been signed...'} when consentSignedAt is set
    - signConsentAction returns {success:true} on valid consent signing
    - updateProfileAction returns {success:false, error} when no session
    - updateProfileAction returns {success:true} with valid profile data
  </behavior>
  <action>
    Create src/__tests__/server-actions.test.ts:

    Mock setup (all vi.mock calls at top level):
    1. vi.mock('server-only', () => ({}))
    2. Mock '@/lib/auth' with mockGetCurrentSession = vi.fn() via vi.hoisted
    3. Mock '@/lib/dal/customers' with mockCreateCustomer, mockUpdateCustomer, mockDeleteCustomer via vi.hoisted, each returning vi.fn()
    4. Mock '@/lib/dal/appointments' with mockCreateAppointment, mockUpdateAppointment, mockDeleteAppointment
    5. Mock '@/lib/dal/audit' with mockLogAudit = vi.fn()
    6. Mock '@/lib/db' with Drizzle query/update shape for portal actions:
       - db.query.customer.findFirst = mockCustomerFindFirst (vi.hoisted)
       - db.query.tattooSession.findFirst = mockTattooSessionFindFirst (vi.hoisted)
       - db.update returns chained .set().where() mock
    7. Mock '@/lib/db/schema' with table column references (customer: { userId, id, email }, tattooSession: { id, customerId, consentSigned, consentSignedAt, consentSignedBy })
    8. Mock 'next/headers' with headers().get() returning '127.0.0.1'
    9. Mock 'next/cache' with revalidatePath = vi.fn()
    10. Mock 'next/server' with after = vi.fn((fn) => fn()) to execute audit callbacks synchronously in tests
    11. Mock 'next/navigation' with redirect = vi.fn()

    Test blocks:

    describe('Customer Actions'):
    - beforeEach: vi.clearAllMocks(), vi.resetModules()
    - Test auth guard: mockGetCurrentSession returns null, expect createCustomerAction to throw /Unauthorized/
    - Test valid create: mockGetCurrentSession returns session with user.id='user-1', mock createCustomer returns {id:'cust-1'}, build FormData with firstName='John', lastName='Doe', email='john@test.com'. Import createCustomerAction dynamically, call it, expect createCustomer called with parsed object
    - Test delete: mockGetCurrentSession returns session, call deleteCustomerAction('cust-1'), expect deleteCustomer called with 'cust-1'

    describe('Appointment Actions'):
    - Test auth guard: same pattern
    - Test valid create: build FormData with scheduledDate, type, customerId, call createAppointmentAction

    describe('Portal Actions - signConsentAction'):
    - Test no session: returns {success:false, error:'You must be logged in to sign consent.'}
    - Test validation failure: send FormData without sessionId, expect {success:false} with error message
    - Test no customer: mockGetCurrentSession returns session, mockCustomerFindFirst returns null, expect {success:false, error:'No linked customer account found.'}
    - Test session not found: mockCustomerFindFirst returns {id:'c1'}, mockTattooSessionFindFirst returns null, expect {success:false, error:'Tattoo session not found.'}
    - Test already signed: mockTattooSessionFindFirst returns { consentSignedAt: new Date() }, expect {success:false, error:'Consent has already been signed...'}
    - Test success: mockTattooSessionFindFirst returns { consentSignedAt: null, id:'s1', customerId:'c1' }, expect {success:true}

    describe('Portal Actions - updateProfileAction'):
    - Test no session: returns {success:false}
    - Test success: FormData with firstName, lastName, call updateProfileAction, expect {success:true}
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && bun run test src/__tests__/server-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/server-actions.test.ts exists and contains at least 14 test cases
    - File contains "describe('Customer Actions'" block with auth guard test
    - File contains "describe('Appointment Actions'" block
    - File contains "describe('Portal Actions - signConsentAction'" block with 6 scenarios
    - File contains "describe('Portal Actions - updateProfileAction'" block
    - File contains vi.mock for '@/lib/auth', '@/lib/dal/customers', '@/lib/dal/appointments', '@/lib/dal/audit', '@/lib/db', 'next/headers', 'next/cache', 'next/server'
    - Portal action tests use .toEqual({success: false, error: expect.stringContaining(...)}) pattern (not throw)
    - Customer/Appointment action tests use expect(...).rejects.toThrow('Unauthorized') pattern
    - Test file passes: `bun run test src/__tests__/server-actions.test.ts` exits 0
  </acceptance_criteria>
  <done>Server actions for customers, appointments, and portal have integration tests covering auth enforcement, input validation, DAL delegation, and error handling patterns (throw for admin actions, {success,error} for portal actions).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upload token endpoint integration tests</name>
  <files>src/__tests__/upload-endpoint.test.ts</files>
  <read_first>
    - src/app/api/upload/token/route.ts (source of truth -- POST handler with STAFF_ROLES check, handleUpload, content type restrictions)
    - src/__tests__/webhook-stripe.test.ts (reference pattern for route handler testing with mocked NextResponse)
    - vitest.config.ts
  </read_first>
  <behavior>
    Upload endpoint tests:
    - POST rejects unauthenticated users (getCurrentSession returns null) via handleUpload's onBeforeGenerateToken throwing 'Unauthorized'
    - POST rejects 'user' role via onBeforeGenerateToken throwing 'Unauthorized'
    - POST accepts 'staff' role through onBeforeGenerateToken
    - POST accepts 'admin' role through onBeforeGenerateToken
    - POST accepts 'super_admin' role through onBeforeGenerateToken
    - onBeforeGenerateToken returns allowedContentTypes with image/jpeg, image/png, image/webp, image/avif, video/mp4
    - onBeforeGenerateToken returns maximumSizeInBytes = 10485760 (10MB)
    - POST returns 400 when handleUpload throws an error
  </behavior>
  <action>
    Create src/__tests__/upload-endpoint.test.ts:

    Mock setup:
    1. vi.mock('server-only', () => ({}))
    2. Mock '@/lib/auth' with mockGetCurrentSession = vi.fn() via vi.hoisted
    3. Mock '@vercel/blob/client' with mockHandleUpload = vi.fn() via vi.hoisted. The mock captures the onBeforeGenerateToken callback so tests can invoke it directly:
       vi.mock('@vercel/blob/client', () => ({
         handleUpload: (...args: unknown[]) => mockHandleUpload(...args),
       }))
    4. Mock 'next/server' with NextResponse.json factory

    Test strategy: Since handleUpload is Vercel's SDK, we cannot directly test the full flow. Instead:
    - Mock handleUpload to capture the options object passed to it
    - Extract the onBeforeGenerateToken callback from the captured args
    - Call onBeforeGenerateToken directly to test auth logic
    - Test that POST returns the jsonResponse from handleUpload on success
    - Test that POST returns 400 when handleUpload throws

    describe('Upload Token Endpoint'):
    - beforeEach: vi.clearAllMocks(), vi.resetModules()
    - Test: handleUpload error returns 400. Mock handleUpload to throw Error('Unauthorized'). Import POST, call with valid request, expect status 400
    - Test: captures onBeforeGenerateToken callback. Mock handleUpload to capture args and call onBeforeGenerateToken. Set session to null. Expect onBeforeGenerateToken to throw 'Unauthorized'
    - Test: user role rejected. Set session with user.role='user'. Expect onBeforeGenerateToken to throw 'Unauthorized'
    - Test: staff role accepted. Set session with user.role='staff'. Expect onBeforeGenerateToken to return object with allowedContentTypes and maximumSizeInBytes
    - Test: admin role accepted. Same with role='admin'
    - Test: return value includes correct allowedContentTypes array ['image/jpeg', 'image/png', 'image/webp', 'image/avif', 'video/mp4']
    - Test: return value includes maximumSizeInBytes = 10 * 1024 * 1024
    - Test: return value includes tokenPayload with userId from session
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && bun run test src/__tests__/upload-endpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/upload-endpoint.test.ts exists and contains at least 6 test cases
    - File contains vi.mock for '@vercel/blob/client' and '@/lib/auth'
    - File contains test that user role is rejected (throws 'Unauthorized')
    - File contains test that staff role is accepted
    - File contains assertion for allowedContentTypes array with 5 types
    - File contains assertion for maximumSizeInBytes = 10485760 (or 10 * 1024 * 1024)
    - File contains assertion for tokenPayload containing userId
    - Test file passes: `bun run test src/__tests__/upload-endpoint.test.ts` exits 0
  </acceptance_criteria>
  <done>Upload token endpoint has integration tests verifying RBAC enforcement (user rejected, staff+ accepted), content type restrictions, file size limit, and error response handling.</done>
</task>

</tasks>

<verification>
Both new test files pass:
```bash
bun run test src/__tests__/server-actions.test.ts src/__tests__/upload-endpoint.test.ts
```
No regressions in existing tests:
```bash
bun run test
```
</verification>

<success_criteria>
- At least 20 new test cases across 2 test files
- Customer, appointment, and portal server actions have auth + validation + DAL delegation tests
- Portal actions tested for {success,error} return pattern (not throw)
- Upload endpoint tested for all 5 RBAC roles
- All existing test files still pass
- Zero test failures on `bun run test`
</success_criteria>

<output>
After completion, create `.planning/phases/12-testing-foundation/12-02-SUMMARY.md`
</output>
