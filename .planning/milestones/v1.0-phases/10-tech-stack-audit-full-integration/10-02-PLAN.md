---
phase: 10-tech-stack-audit-full-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/public/hero-section.tsx
  - src/components/public/gallery-grid.tsx
  - src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
  - src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
  - src/app/(dashboard)/dashboard/contacts/contacts-client.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/(dashboard)/dashboard/payments/columns.tsx
  - src/app/(portal)/portal/appointments/page.tsx
  - src/app/(portal)/portal/page.tsx
  - src/app/(portal)/portal/payments/page.tsx
  - src/app/(portal)/portal/tattoos/page.tsx
  - src/app/(dashboard)/dashboard/customers/[id]/page.tsx
  - src/app/(dashboard)/dashboard/orders/columns.tsx
  - src/components/dashboard/order-detail.tsx
autonomous: true
requirements: [STACK-05, STACK-06]

must_haves:
  truths:
    - "Gallery grid items animate in with staggered reveal when the page loads"
    - "Dashboard and portal date columns show relative timestamps like '3 days ago'"
    - "Absolute dates are preserved in admin detail views and tables where precision matters"
    - "framer-motion AnimatePresence is used beyond the hero section"
  artifacts:
    - path: "src/components/public/gallery-grid.tsx"
      provides: "Staggered gallery item animations"
      contains: "staggerChildren"
    - path: "src/app/(dashboard)/dashboard/customers/customer-list-client.tsx"
      provides: "Relative timestamp in created column"
      contains: "formatDistance"
    - path: "src/app/(portal)/portal/appointments/page.tsx"
      provides: "Relative timestamps for appointment dates"
      contains: "formatDistance"
  key_links:
    - from: "src/components/public/gallery-grid.tsx"
      to: "framer-motion"
      via: "motion.div with variants"
      pattern: "motion\\.div"
    - from: "src/app/(dashboard)/dashboard/customers/customer-list-client.tsx"
      to: "date-fns"
      via: "formatDistance import"
      pattern: "formatDistance"
---

<objective>
Expand framer-motion usage beyond the hero section with gallery stagger animations, and enrich date-fns usage with relative timestamps across dashboard and portal.

Purpose: Extract more value from framer-motion (currently only hero) and date-fns (currently only format()) per STACK-05 and STACK-06.
Output: Animated gallery grid, relative timestamps in dashboard lists and portal pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-stack-audit-full-integration/10-CONTEXT.md
@src/components/public/gallery-grid.tsx
@src/components/public/hero-section.tsx
@src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
@src/app/(portal)/portal/appointments/page.tsx

<interfaces>
<!-- Existing framer-motion usage pattern from hero-section.tsx -->
From src/components/public/hero-section.tsx:
```typescript
import { motion } from "framer-motion";
// Uses motion.div with initial/animate/transition props
// Pattern: initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}
```

<!-- date-fns current usage pattern -->
From all 12 files using date-fns:
```typescript
import { format } from 'date-fns';
// Pattern: format(new Date(row.original.createdAt), 'MMM d, yyyy')
// or: format(new Date(row.original.scheduledDate), 'MMM d, yyyy h:mm a')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand framer-motion with gallery stagger animations</name>
  <files>
    src/components/public/gallery-grid.tsx
  </files>
  <read_first>
    src/components/public/gallery-grid.tsx
    src/components/public/hero-section.tsx
  </read_first>
  <action>
**Gallery grid stagger animation** (per D-11):

Add staggered reveal animation to the gallery grid items in gallery-grid.tsx.

1. Add import: `import { motion, AnimatePresence } from 'framer-motion';`

2. Define animation variants at module level (outside the component):
   ```typescript
   const containerVariants = {
     hidden: {},
     visible: {
       transition: {
         staggerChildren: 0.06,
       },
     },
   };

   const itemVariants = {
     hidden: { opacity: 0, y: 16 },
     visible: {
       opacity: 1,
       y: 0,
       transition: { duration: 0.3, ease: [0.25, 0.1, 0.25, 1] },
     },
   };
   ```

3. Replace the outer `<div className="columns-1 md:columns-2 lg:columns-3 gap-3">` with:
   ```tsx
   <motion.div
     className="columns-1 md:columns-2 lg:columns-3 gap-3"
     variants={containerVariants}
     initial="hidden"
     animate="visible"
     key={`${activeFilters.style}-${activeFilters.placement}-${activeFilters.size}`}
   >
   ```
   The `key` prop forces re-animation when filters change.

4. Wrap each gallery item div (the one with `className="break-inside-avoid mb-3 cursor-pointer group"`) with motion.div:
   ```tsx
   <motion.div
     key={design.id}
     variants={itemVariants}
     className="break-inside-avoid mb-3 cursor-pointer group"
     onClick={() => setLightboxIndex(index)}
     role="button"
     tabIndex={0}
     onKeyDown={(e) => {
       if (e.key === 'Enter' || e.key === ' ') {
         e.preventDefault();
         setLightboxIndex(index);
       }
     }}
   >
   ```
   Remove the original outer `<div>` that had these same props -- motion.div replaces it.

5. Keep the inner `<div className="relative overflow-hidden rounded-lg ...">` and Image unchanged inside the motion.div.

Do NOT wrap the entire gallery in AnimatePresence -- the key prop on the container handles re-animation on filter changes. AnimatePresence would add exit animation complexity that's unnecessary here.
  </action>
  <verify>
    <automated>grep -q 'staggerChildren' src/components/public/gallery-grid.tsx && grep -q 'motion.div' src/components/public/gallery-grid.tsx && grep -q 'itemVariants' src/components/public/gallery-grid.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/public/gallery-grid.tsx contains `import { motion, AnimatePresence } from 'framer-motion'` or `import { motion } from 'framer-motion'`
    - src/components/public/gallery-grid.tsx contains `staggerChildren: 0.06` in containerVariants
    - src/components/public/gallery-grid.tsx contains `motion.div` with `variants={containerVariants}`
    - src/components/public/gallery-grid.tsx contains `itemVariants` with `opacity: 0` in hidden state
    - src/components/public/gallery-grid.tsx still contains the Image component and lightbox integration unchanged
    - The `key` prop on the container includes filter values for re-animation on filter change
  </acceptance_criteria>
  <done>Gallery grid items animate in with staggered reveal; re-animates when filters change</done>
</task>

<task type="auto">
  <name>Task 2: Expand date-fns with relative timestamps across dashboard and portal</name>
  <files>
    src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
    src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
    src/app/(dashboard)/dashboard/contacts/contacts-client.tsx
    src/app/(dashboard)/dashboard/page.tsx
    src/app/(dashboard)/dashboard/payments/columns.tsx
    src/app/(portal)/portal/appointments/page.tsx
    src/app/(portal)/portal/page.tsx
    src/app/(portal)/portal/payments/page.tsx
    src/app/(portal)/portal/tattoos/page.tsx
    src/app/(dashboard)/dashboard/customers/[id]/page.tsx
    src/app/(dashboard)/dashboard/orders/columns.tsx
    src/components/dashboard/order-detail.tsx
  </files>
  <read_first>
    src/app/(dashboard)/dashboard/customers/customer-list-client.tsx
    src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx
    src/app/(dashboard)/dashboard/contacts/contacts-client.tsx
    src/app/(dashboard)/dashboard/page.tsx
    src/app/(dashboard)/dashboard/payments/columns.tsx
    src/app/(portal)/portal/appointments/page.tsx
    src/app/(portal)/portal/page.tsx
    src/app/(portal)/portal/payments/page.tsx
    src/app/(portal)/portal/tattoos/page.tsx
    src/app/(dashboard)/dashboard/customers/[id]/page.tsx
    src/app/(dashboard)/dashboard/orders/columns.tsx
    src/components/dashboard/order-detail.tsx
  </read_first>
  <action>
**Relative timestamps in list views** (per D-13, D-14, D-15):

Add `formatDistance` from date-fns for relative timestamps in list/summary views. Keep absolute dates in detail views and table cells where precision matters.

**Rule:**
- List "Created" columns and summary dates -> `formatDistance(date, new Date(), { addSuffix: true })` (shows "3 days ago")
- Appointment scheduled dates -> keep `format()` with full date+time (precision matters for scheduling)
- Detail pages -> keep `format()` with absolute dates (admin needs exact dates)

For each file, update the import and specific date cells:

1. **customer-list-client.tsx** -- "Created" column:
   - Add to import: `import { format, formatDistance } from 'date-fns';`
   - Change the `createdAt` column cell from:
     `format(new Date(row.original.createdAt), 'MMM d, yyyy')`
     to:
     `formatDistance(new Date(row.original.createdAt), new Date(), { addSuffix: true })`
   - Add a `title` attribute to the wrapping element with the absolute date for hover:
     ```tsx
     cell: ({ row }) => (
       <span title={format(new Date(row.original.createdAt), 'MMM d, yyyy')}>
         {formatDistance(new Date(row.original.createdAt), new Date(), { addSuffix: true })}
       </span>
     ),
     ```

2. **appointment-list-client.tsx** -- keep scheduledDate as absolute (precision matters), but if there is a createdAt column, make it relative. Currently scheduledDate uses `format(new Date(row.original.scheduledDate), 'MMM d, yyyy h:mm a')` -- KEEP this as-is per D-15.

3. **contacts-client.tsx** -- Read file first. If it has a date column with `format()`, apply the same relative timestamp pattern as customer-list (formatDistance with title tooltip). Only for "created" or "submitted" type dates, not for action timestamps.

4. **dashboard/page.tsx (overview)** -- Read file first. If it displays dates (recent customers, recent appointments), apply formatDistance to the "created" or "date" fields that appear in summary cards. Keep appointment scheduled dates absolute.

5. **payments/columns.tsx** -- Payment dates: keep `format()` absolute. Financial records need precise dates per D-15.

6. **Portal pages** (portal/page.tsx, portal/appointments/page.tsx, portal/payments/page.tsx, portal/tattoos/page.tsx):
   - Read each file. For date displays in list/summary views, add formatDistance import and use relative dates.
   - portal/appointments/page.tsx: If showing appointment dates in a list, add relative "booked X days ago" alongside the absolute scheduled date. Keep the scheduled date absolute.
   - portal/payments/page.tsx: Keep payment dates absolute (financial precision).
   - portal/page.tsx: If showing an overview with dates, use relative for "last visit" or "member since" type fields.
   - portal/tattoos/page.tsx: If showing session dates, use relative for list summaries.

7. **customers/[id]/page.tsx** -- Detail view: KEEP absolute dates per D-15. No changes.

8. **orders/columns.tsx** -- "Date" column: Apply relative timestamp same as customers:
   - Change `format(new Date(row.original.createdAt), 'MMM d, yyyy')` to formatDistance with title tooltip.

9. **order-detail.tsx** -- Detail view: Read file. If it shows order date, KEEP absolute per D-15.

For ALL relative dates, always include a `title` attribute with the absolute date so users can hover to see exact date/time.

Import pattern: `import { format, formatDistance } from 'date-fns';` (keep existing `format` import, add `formatDistance`).
  </action>
  <verify>
    <automated>grep -c 'formatDistance' src/app/\(dashboard\)/dashboard/customers/customer-list-client.tsx src/app/\(dashboard\)/dashboard/orders/columns.tsx src/components/public/gallery-grid.tsx 2>/dev/null | grep -v ':0' | wc -l | xargs test 2 -le && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/dashboard/customers/customer-list-client.tsx contains `import { format, formatDistance } from 'date-fns'`
    - src/app/(dashboard)/dashboard/customers/customer-list-client.tsx createdAt column uses `formatDistance` with `addSuffix: true`
    - src/app/(dashboard)/dashboard/customers/customer-list-client.tsx createdAt column has a `title` attribute with absolute date
    - src/app/(dashboard)/dashboard/orders/columns.tsx createdAt column uses `formatDistance` with `addSuffix: true`
    - src/app/(dashboard)/dashboard/appointments/appointment-list-client.tsx scheduledDate column STILL uses `format()` (not changed to relative)
    - src/app/(dashboard)/dashboard/payments/columns.tsx date column STILL uses `format()` (financial precision kept)
    - At least 2 files in portal/ directory contain `formatDistance` import
    - All relative date displays include a `title` attribute with absolute date fallback
  </acceptance_criteria>
  <done>Dashboard and portal list views show relative timestamps ("3 days ago"); absolute dates preserved where precision matters; hover shows exact date</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run test` passes
- Gallery grid uses motion.div with staggerChildren
- At least 4 files use formatDistance from date-fns
- Appointment scheduled dates and payment dates remain absolute
</verification>

<success_criteria>
1. framer-motion AnimatePresence/stagger used in gallery grid (beyond hero-only)
2. date-fns formatDistance used in dashboard customer list, order list, and portal pages
3. Absolute dates preserved in detail views, appointment scheduling, and payment records (per D-15)
4. All relative dates have title tooltip with absolute date
5. Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-stack-audit-full-integration/10-02-SUMMARY.md`
</output>
