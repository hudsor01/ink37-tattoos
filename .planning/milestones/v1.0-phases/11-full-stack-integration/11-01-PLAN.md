---
phase: 11-full-stack-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/calendar.tsx
  - src/components/ui/date-picker.tsx
  - src/components/ui/drawer.tsx
  - src/components/ui/command.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/radio-group.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/toggle.tsx
  - src/components/ui/toggle-group.tsx
  - src/components/ui/input-otp.tsx
  - src/components/ui/hover-card.tsx
  - src/components/ui/form.tsx
  - src/components/ui/chart.tsx
  - src/components/ui/sidebar.tsx
  - src/components/public/gallery-grid.tsx
autonomous: true
requirements:
  - STACK-11
  - STACK-12

must_haves:
  truths:
    - "All 16 missing shadcn/ui components are installed and importable"
    - "Context.Provider pattern replaced with React 19 Context-as-provider in form.tsx, chart.tsx, sidebar.tsx"
    - "gallery-grid.tsx uses LazyMotion+m instead of full motion import (saves ~19kb)"
  artifacts:
    - path: "src/components/ui/calendar.tsx"
      provides: "Calendar component"
    - path: "src/components/ui/date-picker.tsx"
      provides: "Date picker component"
    - path: "src/components/ui/drawer.tsx"
      provides: "Drawer component"
    - path: "src/components/ui/command.tsx"
      provides: "Command palette component"
    - path: "src/components/ui/popover.tsx"
      provides: "Popover component"
    - path: "src/components/ui/checkbox.tsx"
      provides: "Checkbox component"
    - path: "src/components/ui/progress.tsx"
      provides: "Progress bar component"
    - path: "src/components/ui/avatar.tsx"
      provides: "Avatar component"
  key_links:
    - from: "src/components/ui/date-picker.tsx"
      to: "src/components/ui/calendar.tsx"
      via: "import Calendar"
      pattern: "import.*Calendar.*from.*calendar"
    - from: "src/components/ui/date-picker.tsx"
      to: "src/components/ui/popover.tsx"
      via: "import Popover"
      pattern: "import.*Popover.*from.*popover"
---

<objective>
Install all 16 missing shadcn/ui components, update React Context.Provider to React 19 Context-as-provider pattern, and fix framer-motion bundle optimization in gallery-grid.tsx.

Purpose: Complete the UI component library so downstream plans can use Calendar, DatePicker, Command, Checkbox etc. Fix the ~19kb framer-motion bundle bloat. Modernize Context patterns to React 19.
Output: 16 new UI components installed, 3 files updated to remove .Provider, gallery-grid.tsx optimized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-stack-integration/11-RESEARCH-A.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install all missing shadcn/ui components and fix Context.Provider pattern</name>
  <files>
    src/components/ui/calendar.tsx, src/components/ui/date-picker.tsx, src/components/ui/drawer.tsx,
    src/components/ui/command.tsx, src/components/ui/popover.tsx, src/components/ui/checkbox.tsx,
    src/components/ui/progress.tsx, src/components/ui/avatar.tsx, src/components/ui/scroll-area.tsx,
    src/components/ui/collapsible.tsx, src/components/ui/radio-group.tsx, src/components/ui/slider.tsx,
    src/components/ui/toggle.tsx, src/components/ui/toggle-group.tsx, src/components/ui/input-otp.tsx,
    src/components/ui/hover-card.tsx, src/components/ui/form.tsx, src/components/ui/chart.tsx,
    src/components/ui/sidebar.tsx
  </files>
  <read_first>
    src/components/ui/form.tsx
    src/components/ui/chart.tsx
    src/components/ui/sidebar.tsx
    src/components/ui/tooltip.tsx
    components.json
  </read_first>
  <action>
    1. Install all 16 missing HIGH and MEDIUM priority shadcn/ui components:
       ```
       bun x shadcn@latest add calendar date-picker drawer command popover checkbox progress avatar scroll-area collapsible radio-group slider toggle toggle-group input-otp hover-card
       ```

    2. After installation, update React 19 Context-as-provider pattern in 3 files:

       **form.tsx** -- Replace `<FormFieldContext.Provider value={{ name: props.name }}>` with `<FormFieldContext value={{ name: props.name }}>` and `<FormItemContext.Provider value={{ id }}>` with `<FormItemContext value={{ id }}>`. Remove all `.Provider` suffixes from closing tags too.

       **chart.tsx** -- Replace `<ChartContext.Provider value={{ config }}>` with `<ChartContext value={{ config }}>`. Remove `.Provider` from closing tag.

       **sidebar.tsx** -- Replace `<SidebarContext.Provider value={contextValue}>` with `<SidebarContext value={contextValue}>`. Remove `.Provider` from closing tag.

       **DO NOT** touch tooltip.tsx -- `TooltipPrimitive.Provider` is a Base UI component API, not React Context. Changing it would break the tooltip.

    3. Verify all components export correctly by checking each file exists and has a default or named export.
  </action>
  <acceptance_criteria>
    - grep -r "from.*@/components/ui/calendar" should find the calendar component importable
    - grep -c "\.Provider" src/components/ui/form.tsx returns 0
    - grep -c "\.Provider" src/components/ui/chart.tsx returns 0
    - grep -c "\.Provider" src/components/ui/sidebar.tsx returns 0
    - grep "TooltipPrimitive.Provider" src/components/ui/tooltip.tsx still exists (NOT changed)
    - ls src/components/ui/calendar.tsx src/components/ui/date-picker.tsx src/components/ui/drawer.tsx src/components/ui/command.tsx src/components/ui/popover.tsx src/components/ui/checkbox.tsx src/components/ui/progress.tsx src/components/ui/avatar.tsx src/components/ui/scroll-area.tsx -- all exist
  </acceptance_criteria>
  <verify>
    <automated>ls src/components/ui/{calendar,date-picker,drawer,command,popover,checkbox,progress,avatar,scroll-area,collapsible,radio-group,slider,toggle,toggle-group,input-otp,hover-card}.tsx | wc -l</automated>
  </verify>
  <done>All 16 shadcn/ui components installed. Context.Provider replaced with Context-as-provider in form.tsx, chart.tsx, sidebar.tsx. tooltip.tsx TooltipPrimitive.Provider left unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix framer-motion bundle in gallery-grid.tsx (LazyMotion+m pattern)</name>
  <files>src/components/public/gallery-grid.tsx</files>
  <read_first>
    src/components/public/gallery-grid.tsx
    src/components/public/hero-section.tsx
    src/components/page-transition.tsx
  </read_first>
  <action>
    In gallery-grid.tsx, replace the full `motion` import with the optimized `LazyMotion + m + domAnimation` pattern that hero-section.tsx and page-transition.tsx already use:

    1. Change import from `import { motion } from 'framer-motion'` to `import { LazyMotion, domAnimation, m } from 'framer-motion'`
    2. If AnimatePresence is also imported, keep it -- it works with both patterns.
    3. Replace all `motion.div` with `m.div`, `motion.img` with `m.img`, etc.
    4. Wrap the outermost animated container with `<LazyMotion features={domAnimation}>` ... `</LazyMotion>`
    5. Verify the existing `variants` objects (containerVariants, itemVariants) work unchanged -- they are compatible with both `motion` and `m` components.

    Reference hero-section.tsx for the exact pattern this project uses.

    This saves approximately 19kb gzipped from the client bundle because `m` only loads animation features lazily through the `LazyMotion` provider instead of bundling the full motion runtime.
  </action>
  <acceptance_criteria>
    - grep "from 'framer-motion'" src/components/public/gallery-grid.tsx shows LazyMotion, domAnimation, m (not motion)
    - grep -c "motion\." src/components/public/gallery-grid.tsx returns 0 (no full motion component usage)
    - grep -c "m\." src/components/public/gallery-grid.tsx returns at least 1
    - grep "LazyMotion" src/components/public/gallery-grid.tsx returns a match
    - bun run build completes without errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/richard/Developer/ink37-tattoos && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>gallery-grid.tsx uses LazyMotion+m pattern. Zero imports of full `motion` component from framer-motion anywhere in codebase. Build passes.</done>
</task>

</tasks>

<verification>
- `ls src/components/ui/*.tsx | wc -l` shows 39 total UI components (23 existing + 16 new)
- `grep -r "\.Provider" src/components/ui/{form,chart,sidebar}.tsx` returns 0 matches
- `grep -c "motion\." src/components/public/gallery-grid.tsx` returns 0
- `bun run build` succeeds
</verification>

<success_criteria>
All 16 missing shadcn/ui components installed and importable. React 19 Context-as-provider pattern applied to form.tsx, chart.tsx, sidebar.tsx (NOT tooltip.tsx). gallery-grid.tsx uses LazyMotion+m saving ~19kb. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-stack-integration/11-01-SUMMARY.md`
</output>
